name: Set up webapp
description: Install the Node toolchain and locked frontend dependencies for Rust builds.
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: .node-version
        cache: npm
        cache-dependency-path: package-lock.json
    - run: npm ci
      shell: bash
