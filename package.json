{
  "name": "waymark-js",
  "private": true,
  "type": "module",
  "workspaces": [
    "js/app/*",
    "js/lib/*"
  ],
  "engines": {
    "node": ">=24.13.0"
  },
  "scripts": {
    "lint": "prettier --check package.json package-lock.json && npm run lint --workspaces",
    "lint:fix": "prettier --write package.json package-lock.json && npm run lint:fix --workspaces"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
