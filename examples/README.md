# Waymark examples

- [`python`](./python) is the FastAPI application backed by durable Python
  workers and Postgres.
- [`javascript`](./javascript) is a Next.js application that compiles a typed
  TypeScript workflow and executes it through the Waymark VM.

Each directory is a standalone example with its own README, tests, Dockerfile,
and Docker Compose configuration.
