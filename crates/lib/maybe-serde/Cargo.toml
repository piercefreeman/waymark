[package]
name = "waymark-maybe-serde"
edition = "2024"
version.workspace = true
publish.workspace = true

[features]
default = []

serde = ["dep:serde"]

[dependencies]
serde = { workspace = true, optional = true }
