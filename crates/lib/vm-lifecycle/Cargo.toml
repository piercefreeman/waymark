[package]
name = "waymark-vm-lifecycle"
edition = "2024"
version.workspace = true
publish.workspace = true

[dependencies]
chrono = { workspace = true, default-features = false, features = ["clock"] }
