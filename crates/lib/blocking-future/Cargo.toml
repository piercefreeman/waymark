[package]
name = "waymark-blocking-future"
edition = "2024"
version.workspace = true
publish.workspace = true

[dependencies]
tokio = { workspace = true, features = ["rt"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "sync"] }
