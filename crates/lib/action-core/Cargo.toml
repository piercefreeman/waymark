[package]
name = "waymark-action-core"
edition = "2024"
version.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true, optional = true }

[features]
serde = ["dep:serde", "serde/derive"]
