[package]
name = "waymark-observability-store-postgres-migrations"
edition = "2024"
version.workspace = true
publish.workspace = true

[dependencies]
sqlx = { workspace = true, features = ["macros", "migrate"] }
