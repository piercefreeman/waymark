// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.29.0
// source: messages.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientUnaryCall,
  type handleBidiStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { ActionRuntime, actionRuntimeFromJSON, actionRuntimeToJSON } from "./action.js";
import { NullValue, nullValueFromJSON, nullValueToJSON } from "./google/protobuf/struct.js";

export const protobufPackage = "waymark.messages";

export enum MessageKind {
  MESSAGE_KIND_UNSPECIFIED = 0,
  /** MESSAGE_KIND_ACTION_DISPATCH - Server -> Worker: execute this action */
  MESSAGE_KIND_ACTION_DISPATCH = 1,
  /** MESSAGE_KIND_ACTION_RESULT - Worker -> Server: here's the result */
  MESSAGE_KIND_ACTION_RESULT = 2,
  /** MESSAGE_KIND_ACK - Worker -> Server: message received */
  MESSAGE_KIND_ACK = 3,
  /** MESSAGE_KIND_HEARTBEAT - Bidirectional: keep-alive signal */
  MESSAGE_KIND_HEARTBEAT = 4,
  /** MESSAGE_KIND_WORKER_HELLO - Worker -> Server: handshake on connect */
  MESSAGE_KIND_WORKER_HELLO = 5,
  UNRECOGNIZED = -1,
}

export function messageKindFromJSON(object: any): MessageKind {
  switch (object) {
    case 0:
    case "MESSAGE_KIND_UNSPECIFIED":
      return MessageKind.MESSAGE_KIND_UNSPECIFIED;
    case 1:
    case "MESSAGE_KIND_ACTION_DISPATCH":
      return MessageKind.MESSAGE_KIND_ACTION_DISPATCH;
    case 2:
    case "MESSAGE_KIND_ACTION_RESULT":
      return MessageKind.MESSAGE_KIND_ACTION_RESULT;
    case 3:
    case "MESSAGE_KIND_ACK":
      return MessageKind.MESSAGE_KIND_ACK;
    case 4:
    case "MESSAGE_KIND_HEARTBEAT":
      return MessageKind.MESSAGE_KIND_HEARTBEAT;
    case 5:
    case "MESSAGE_KIND_WORKER_HELLO":
      return MessageKind.MESSAGE_KIND_WORKER_HELLO;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MessageKind.UNRECOGNIZED;
  }
}

export function messageKindToJSON(object: MessageKind): string {
  switch (object) {
    case MessageKind.MESSAGE_KIND_UNSPECIFIED:
      return "MESSAGE_KIND_UNSPECIFIED";
    case MessageKind.MESSAGE_KIND_ACTION_DISPATCH:
      return "MESSAGE_KIND_ACTION_DISPATCH";
    case MessageKind.MESSAGE_KIND_ACTION_RESULT:
      return "MESSAGE_KIND_ACTION_RESULT";
    case MessageKind.MESSAGE_KIND_ACK:
      return "MESSAGE_KIND_ACK";
    case MessageKind.MESSAGE_KIND_HEARTBEAT:
      return "MESSAGE_KIND_HEARTBEAT";
    case MessageKind.MESSAGE_KIND_WORKER_HELLO:
      return "MESSAGE_KIND_WORKER_HELLO";
    case MessageKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ScheduleType {
  SCHEDULE_TYPE_UNSPECIFIED = 0,
  SCHEDULE_TYPE_CRON = 1,
  SCHEDULE_TYPE_INTERVAL = 2,
  UNRECOGNIZED = -1,
}

export function scheduleTypeFromJSON(object: any): ScheduleType {
  switch (object) {
    case 0:
    case "SCHEDULE_TYPE_UNSPECIFIED":
      return ScheduleType.SCHEDULE_TYPE_UNSPECIFIED;
    case 1:
    case "SCHEDULE_TYPE_CRON":
      return ScheduleType.SCHEDULE_TYPE_CRON;
    case 2:
    case "SCHEDULE_TYPE_INTERVAL":
      return ScheduleType.SCHEDULE_TYPE_INTERVAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ScheduleType.UNRECOGNIZED;
  }
}

export function scheduleTypeToJSON(object: ScheduleType): string {
  switch (object) {
    case ScheduleType.SCHEDULE_TYPE_UNSPECIFIED:
      return "SCHEDULE_TYPE_UNSPECIFIED";
    case ScheduleType.SCHEDULE_TYPE_CRON:
      return "SCHEDULE_TYPE_CRON";
    case ScheduleType.SCHEDULE_TYPE_INTERVAL:
      return "SCHEDULE_TYPE_INTERVAL";
    case ScheduleType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ScheduleStatus {
  SCHEDULE_STATUS_UNSPECIFIED = 0,
  SCHEDULE_STATUS_ACTIVE = 1,
  SCHEDULE_STATUS_PAUSED = 2,
  UNRECOGNIZED = -1,
}

export function scheduleStatusFromJSON(object: any): ScheduleStatus {
  switch (object) {
    case 0:
    case "SCHEDULE_STATUS_UNSPECIFIED":
      return ScheduleStatus.SCHEDULE_STATUS_UNSPECIFIED;
    case 1:
    case "SCHEDULE_STATUS_ACTIVE":
      return ScheduleStatus.SCHEDULE_STATUS_ACTIVE;
    case 2:
    case "SCHEDULE_STATUS_PAUSED":
      return ScheduleStatus.SCHEDULE_STATUS_PAUSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ScheduleStatus.UNRECOGNIZED;
  }
}

export function scheduleStatusToJSON(object: ScheduleStatus): string {
  switch (object) {
    case ScheduleStatus.SCHEDULE_STATUS_UNSPECIFIED:
      return "SCHEDULE_STATUS_UNSPECIFIED";
    case ScheduleStatus.SCHEDULE_STATUS_ACTIVE:
      return "SCHEDULE_STATUS_ACTIVE";
    case ScheduleStatus.SCHEDULE_STATUS_PAUSED:
      return "SCHEDULE_STATUS_PAUSED";
    case ScheduleStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Universal transport wrapper for all worker bridge messages.
 * The `kind` field indicates how to decode the `payload`.
 */
export interface Envelope {
  /** Unique ID for correlation (ack, response) */
  deliveryId: bigint;
  /** Optional logical grouping */
  partitionId: number;
  /** Message type discriminator */
  kind: MessageKind;
  /** Encoded message (ActionDispatch, ActionResult, etc.) */
  payload: Buffer;
}

/** Server -> Worker: instruction to execute an action */
export interface ActionDispatch {
  /** Unique action identifier */
  actionId: string;
  /** Workflow instance this action belongs to */
  instanceId: string;
  /** Sequence number within instance */
  sequence: number;
  /** Action execution details */
  actionName: string;
  /** Module containing the action */
  moduleName: string;
  /** Keyword arguments for the action */
  kwargs:
    | WorkflowArguments
    | undefined;
  /** Execution policies */
  timeoutSeconds?: number | undefined;
  maxRetries?: number | undefined;
  attemptNumber?:
    | number
    | undefined;
  /** UUID for result correlation */
  dispatchToken?:
    | string
    | undefined;
  /** Opaque server correlation metadata; workers echo it untouched */
  metadata: Buffer;
  runtime: ActionRuntime;
}

/** Worker -> Server: result of action execution */
export interface ActionResult {
  actionId: string;
  success: boolean;
  /** Result or error details */
  payload:
    | WorkflowArguments
    | undefined;
  /** Perf counter at execution start */
  workerStartNs: bigint;
  /** Perf counter at execution end */
  workerEndNs: bigint;
  dispatchToken?:
    | string
    | undefined;
  /** Exception type if success=false */
  errorType?:
    | string
    | undefined;
  /** Exception message if success=false */
  errorMessage?:
    | string
    | undefined;
  /** Opaque server correlation metadata echoed from the dispatch */
  metadata: Buffer;
}

/** Worker -> Server: acknowledgment of message receipt */
export interface Ack {
  ackedDeliveryId: bigint;
}

/** Worker -> Server: initial handshake when connecting */
export interface WorkerHello {
  workerId: bigint;
  runtime: ActionRuntime;
}

export interface WorkflowArgumentValue {
  kind:
    | { $case: "primitive"; value: PrimitiveWorkflowArgument }
    | { $case: "basemodel"; value: BaseModelWorkflowArgument }
    | { $case: "exception"; value: WorkflowErrorValue }
    | { $case: "listValue"; value: WorkflowListArgument }
    | { $case: "tupleValue"; value: WorkflowTupleArgument }
    | { $case: "dictValue"; value: WorkflowDictArgument }
    | undefined;
}

export interface WorkflowArgument {
  key: string;
  value: WorkflowArgumentValue | undefined;
}

export interface WorkflowArguments {
  arguments: WorkflowArgument[];
}

export interface PrimitiveWorkflowArgument {
  kind:
    | { $case: "stringValue"; value: string }
    | { $case: "doubleValue"; value: number }
    | { $case: "intValue"; value: bigint }
    | { $case: "boolValue"; value: boolean }
    | { $case: "nullValue"; value: NullValue }
    | undefined;
}

/** Pydantic BaseModel or similar structured types */
export interface BaseModelWorkflowArgument {
  module: string;
  name: string;
  data: WorkflowDictArgument | undefined;
}

/** Serialized exception for error propagation */
export interface WorkflowErrorValue {
  type: string;
  module: string;
  message: string;
  traceback: string;
  values:
    | WorkflowDictArgument
    | undefined;
  /**
   * Exception class hierarchy (MRO) for proper except matching.
   * e.g., for KeyError: ["KeyError", "LookupError", "Exception", "BaseException"]
   * This allows `except LookupError:` to catch KeyError.
   */
  typeHierarchy: string[];
}

export interface WorkflowListArgument {
  items: WorkflowArgumentValue[];
}

export interface WorkflowTupleArgument {
  items: WorkflowArgumentValue[];
}

export interface WorkflowDictArgument {
  entries: WorkflowArgument[];
}

/** Workflow registration containing the IR program */
export interface WorkflowRegistration {
  workflowName: string;
  /** Serialized IR program (from ast.proto) */
  ir: Buffer;
  /** Hash of the IR for immutability checks */
  irHash: string;
  /** User-defined version identifier */
  workflowVersion: string;
  initialContext:
    | WorkflowArguments
    | undefined;
  /** Whether multiple instances can run */
  concurrent: boolean;
  /** Priority for queue ordering (higher values are processed first, default 0) */
  priority?: number | undefined;
}

export interface RegisterWorkflowRequest {
  registration: WorkflowRegistration | undefined;
}

export interface RegisterWorkflowResponse {
  workflowVersionId: string;
  workflowInstanceId: string;
}

export interface RegisterWorkflowBatchRequest {
  registration:
    | WorkflowRegistration
    | undefined;
  /** Total number of instances to create when inputs_list is empty. */
  count: number;
  /** Base inputs to apply to each instance when inputs_list is empty. */
  inputs:
    | WorkflowArguments
    | undefined;
  /** Per-instance inputs (overrides count/inputs when provided). */
  inputsList: WorkflowArguments[];
  /** Batch size for database inserts. */
  batchSize: number;
  /** Whether to return instance IDs in the response. */
  includeInstanceIds: boolean;
}

export interface RegisterWorkflowBatchResponse {
  workflowVersionId: string;
  workflowInstanceIds: string[];
  queued: number;
}

export interface WaitForInstanceRequest {
  instanceId: string;
  pollIntervalSecs: number;
}

export interface WaitForInstanceResponse {
  payload: Buffer;
}

/** Client -> server messages for workflow execution streaming. */
export interface WorkflowStreamRequest {
  kind:
    | { $case: "registration"; value: WorkflowRegistration }
    | { $case: "actionResult"; value: ActionResult }
    | undefined;
  /** Skip sleep/backoff delays in in-memory execution (e.g., pytest). */
  skipSleep: boolean;
}

/** Server -> client final workflow result payload. */
export interface WorkflowExecutionResult {
  payload: Buffer;
}

/** Server -> client messages for workflow execution streaming. */
export interface WorkflowStreamResponse {
  kind:
    | { $case: "actionDispatch"; value: ActionDispatch }
    | { $case: "workflowResult"; value: WorkflowExecutionResult }
    | undefined;
}

export interface ScheduleDefinition {
  type: ScheduleType;
  /** For cron: the cron expression (e.g., "0 * * * *") */
  cronExpression: string;
  /** For interval: duration in seconds */
  intervalSeconds: bigint;
  /** Optional: jitter window in seconds (random 0..jitter_seconds) */
  jitterSeconds: bigint;
}

export interface RegisterScheduleRequest {
  workflowName: string;
  schedule:
    | ScheduleDefinition
    | undefined;
  /** Optional: inputs to pass to each scheduled run */
  inputs:
    | WorkflowArguments
    | undefined;
  /**
   * Optional: workflow registration to register the DAG before scheduling.
   * If provided, the workflow version will be registered (or updated) before
   * the schedule is created. This ensures the workflow can execute when the
   * schedule fires.
   */
  registration:
    | WorkflowRegistration
    | undefined;
  /**
   * Required: unique name for this schedule. Allows multiple schedules per workflow
   * with different inputs. Must be unique within a workflow.
   */
  scheduleName: string;
  /** Priority for queue ordering (higher values are processed first, default 0) */
  priority?:
    | number
    | undefined;
  /**
   * If false (default), skip creating a new instance when one is already running
   * for this schedule. If true, always create a new instance.
   */
  allowDuplicate?: boolean | undefined;
}

export interface RegisterScheduleResponse {
  scheduleId: string;
  /** The computed next_run_at timestamp (ISO 8601) */
  nextRunAt: string;
}

export interface UpdateScheduleStatusRequest {
  workflowName: string;
  status: ScheduleStatus;
  /** Required: name of the schedule to update. */
  scheduleName: string;
}

export interface UpdateScheduleStatusResponse {
  success: boolean;
}

export interface DeleteScheduleRequest {
  workflowName: string;
  /** Required: name of the schedule to delete. */
  scheduleName: string;
}

export interface DeleteScheduleResponse {
  success: boolean;
}

export interface ListSchedulesRequest {
  /** Optional filter by status ("active", "paused"). If empty, returns all non-deleted. */
  statusFilter?: string | undefined;
}

export interface ScheduleInfo {
  id: string;
  workflowName: string;
  scheduleType: ScheduleType;
  /** Empty if interval-based */
  cronExpression: string;
  /** 0 if cron-based */
  intervalSeconds: bigint;
  status: ScheduleStatus;
  /** ISO 8601 timestamp */
  nextRunAt: string;
  /** ISO 8601 timestamp, empty if never run */
  lastRunAt: string;
  /** Empty if never run */
  lastInstanceId: string;
  /** ISO 8601 timestamp */
  createdAt: string;
  /** ISO 8601 timestamp */
  updatedAt: string;
  /** Name of this schedule (allows multiple per workflow) */
  scheduleName: string;
  /** 0 if no jitter configured */
  jitterSeconds: bigint;
  /** Whether duplicate instances are allowed */
  allowDuplicate: boolean;
}

export interface ListSchedulesResponse {
  schedules: ScheduleInfo[];
}

function createBaseEnvelope(): Envelope {
  return { deliveryId: 0n, partitionId: 0, kind: 0, payload: Buffer.alloc(0) };
}

export const Envelope: MessageFns<Envelope> = {
  encode(message: Envelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deliveryId !== 0n) {
      if (BigInt.asUintN(64, message.deliveryId) !== message.deliveryId) {
        throw new globalThis.Error("value provided for field message.deliveryId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.deliveryId);
    }
    if (message.partitionId !== 0) {
      writer.uint32(16).uint32(message.partitionId);
    }
    if (message.kind !== 0) {
      writer.uint32(24).int32(message.kind);
    }
    if (message.payload.length !== 0) {
      writer.uint32(34).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Envelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deliveryId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.partitionId = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Envelope {
    return {
      deliveryId: isSet(object.deliveryId)
        ? BigInt(object.deliveryId)
        : isSet(object.delivery_id)
        ? BigInt(object.delivery_id)
        : 0n,
      partitionId: isSet(object.partitionId)
        ? globalThis.Number(object.partitionId)
        : isSet(object.partition_id)
        ? globalThis.Number(object.partition_id)
        : 0,
      kind: isSet(object.kind) ? messageKindFromJSON(object.kind) : 0,
      payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0),
    };
  },

  toJSON(message: Envelope): unknown {
    const obj: any = {};
    if (message.deliveryId !== 0n) {
      obj.deliveryId = message.deliveryId.toString();
    }
    if (message.partitionId !== 0) {
      obj.partitionId = Math.round(message.partitionId);
    }
    if (message.kind !== 0) {
      obj.kind = messageKindToJSON(message.kind);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Envelope>, I>>(base?: I): Envelope {
    return Envelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Envelope>, I>>(object: I): Envelope {
    const message = createBaseEnvelope();
    message.deliveryId = (object.deliveryId !== undefined && object.deliveryId !== null)
      ? BigInt(object.deliveryId)
      : 0n;
    message.partitionId = object.partitionId ?? 0;
    message.kind = object.kind ?? 0;
    message.payload = object.payload ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseActionDispatch(): ActionDispatch {
  return {
    actionId: "",
    instanceId: "",
    sequence: 0,
    actionName: "",
    moduleName: "",
    kwargs: undefined,
    timeoutSeconds: undefined,
    maxRetries: undefined,
    attemptNumber: undefined,
    dispatchToken: undefined,
    metadata: Buffer.alloc(0),
    runtime: 0,
  };
}

export const ActionDispatch: MessageFns<ActionDispatch> = {
  encode(message: ActionDispatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actionId !== "") {
      writer.uint32(10).string(message.actionId);
    }
    if (message.instanceId !== "") {
      writer.uint32(18).string(message.instanceId);
    }
    if (message.sequence !== 0) {
      writer.uint32(24).uint32(message.sequence);
    }
    if (message.actionName !== "") {
      writer.uint32(34).string(message.actionName);
    }
    if (message.moduleName !== "") {
      writer.uint32(42).string(message.moduleName);
    }
    if (message.kwargs !== undefined) {
      WorkflowArguments.encode(message.kwargs, writer.uint32(50).fork()).join();
    }
    if (message.timeoutSeconds !== undefined) {
      writer.uint32(56).uint32(message.timeoutSeconds);
    }
    if (message.maxRetries !== undefined) {
      writer.uint32(64).uint32(message.maxRetries);
    }
    if (message.attemptNumber !== undefined) {
      writer.uint32(72).uint32(message.attemptNumber);
    }
    if (message.dispatchToken !== undefined) {
      writer.uint32(82).string(message.dispatchToken);
    }
    if (message.metadata.length !== 0) {
      writer.uint32(90).bytes(message.metadata);
    }
    if (message.runtime !== 0) {
      writer.uint32(96).int32(message.runtime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionDispatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionDispatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sequence = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.actionName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.moduleName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kwargs = WorkflowArguments.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.timeoutSeconds = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxRetries = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.attemptNumber = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.dispatchToken = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.metadata = Buffer.from(reader.bytes());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.runtime = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionDispatch {
    return {
      actionId: isSet(object.actionId)
        ? globalThis.String(object.actionId)
        : isSet(object.action_id)
        ? globalThis.String(object.action_id)
        : "",
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
      actionName: isSet(object.actionName)
        ? globalThis.String(object.actionName)
        : isSet(object.action_name)
        ? globalThis.String(object.action_name)
        : "",
      moduleName: isSet(object.moduleName)
        ? globalThis.String(object.moduleName)
        : isSet(object.module_name)
        ? globalThis.String(object.module_name)
        : "",
      kwargs: isSet(object.kwargs) ? WorkflowArguments.fromJSON(object.kwargs) : undefined,
      timeoutSeconds: isSet(object.timeoutSeconds)
        ? globalThis.Number(object.timeoutSeconds)
        : isSet(object.timeout_seconds)
        ? globalThis.Number(object.timeout_seconds)
        : undefined,
      maxRetries: isSet(object.maxRetries)
        ? globalThis.Number(object.maxRetries)
        : isSet(object.max_retries)
        ? globalThis.Number(object.max_retries)
        : undefined,
      attemptNumber: isSet(object.attemptNumber)
        ? globalThis.Number(object.attemptNumber)
        : isSet(object.attempt_number)
        ? globalThis.Number(object.attempt_number)
        : undefined,
      dispatchToken: isSet(object.dispatchToken)
        ? globalThis.String(object.dispatchToken)
        : isSet(object.dispatch_token)
        ? globalThis.String(object.dispatch_token)
        : undefined,
      metadata: isSet(object.metadata) ? Buffer.from(bytesFromBase64(object.metadata)) : Buffer.alloc(0),
      runtime: isSet(object.runtime) ? actionRuntimeFromJSON(object.runtime) : 0,
    };
  },

  toJSON(message: ActionDispatch): unknown {
    const obj: any = {};
    if (message.actionId !== "") {
      obj.actionId = message.actionId;
    }
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.actionName !== "") {
      obj.actionName = message.actionName;
    }
    if (message.moduleName !== "") {
      obj.moduleName = message.moduleName;
    }
    if (message.kwargs !== undefined) {
      obj.kwargs = WorkflowArguments.toJSON(message.kwargs);
    }
    if (message.timeoutSeconds !== undefined) {
      obj.timeoutSeconds = Math.round(message.timeoutSeconds);
    }
    if (message.maxRetries !== undefined) {
      obj.maxRetries = Math.round(message.maxRetries);
    }
    if (message.attemptNumber !== undefined) {
      obj.attemptNumber = Math.round(message.attemptNumber);
    }
    if (message.dispatchToken !== undefined) {
      obj.dispatchToken = message.dispatchToken;
    }
    if (message.metadata.length !== 0) {
      obj.metadata = base64FromBytes(message.metadata);
    }
    if (message.runtime !== 0) {
      obj.runtime = actionRuntimeToJSON(message.runtime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionDispatch>, I>>(base?: I): ActionDispatch {
    return ActionDispatch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionDispatch>, I>>(object: I): ActionDispatch {
    const message = createBaseActionDispatch();
    message.actionId = object.actionId ?? "";
    message.instanceId = object.instanceId ?? "";
    message.sequence = object.sequence ?? 0;
    message.actionName = object.actionName ?? "";
    message.moduleName = object.moduleName ?? "";
    message.kwargs = (object.kwargs !== undefined && object.kwargs !== null)
      ? WorkflowArguments.fromPartial(object.kwargs)
      : undefined;
    message.timeoutSeconds = object.timeoutSeconds ?? undefined;
    message.maxRetries = object.maxRetries ?? undefined;
    message.attemptNumber = object.attemptNumber ?? undefined;
    message.dispatchToken = object.dispatchToken ?? undefined;
    message.metadata = object.metadata ?? Buffer.alloc(0);
    message.runtime = object.runtime ?? 0;
    return message;
  },
};

function createBaseActionResult(): ActionResult {
  return {
    actionId: "",
    success: false,
    payload: undefined,
    workerStartNs: 0n,
    workerEndNs: 0n,
    dispatchToken: undefined,
    errorType: undefined,
    errorMessage: undefined,
    metadata: Buffer.alloc(0),
  };
}

export const ActionResult: MessageFns<ActionResult> = {
  encode(message: ActionResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actionId !== "") {
      writer.uint32(10).string(message.actionId);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    if (message.payload !== undefined) {
      WorkflowArguments.encode(message.payload, writer.uint32(26).fork()).join();
    }
    if (message.workerStartNs !== 0n) {
      if (BigInt.asUintN(64, message.workerStartNs) !== message.workerStartNs) {
        throw new globalThis.Error("value provided for field message.workerStartNs of type uint64 too large");
      }
      writer.uint32(32).uint64(message.workerStartNs);
    }
    if (message.workerEndNs !== 0n) {
      if (BigInt.asUintN(64, message.workerEndNs) !== message.workerEndNs) {
        throw new globalThis.Error("value provided for field message.workerEndNs of type uint64 too large");
      }
      writer.uint32(40).uint64(message.workerEndNs);
    }
    if (message.dispatchToken !== undefined) {
      writer.uint32(50).string(message.dispatchToken);
    }
    if (message.errorType !== undefined) {
      writer.uint32(58).string(message.errorType);
    }
    if (message.errorMessage !== undefined) {
      writer.uint32(66).string(message.errorMessage);
    }
    if (message.metadata.length !== 0) {
      writer.uint32(74).bytes(message.metadata);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = WorkflowArguments.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.workerStartNs = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.workerEndNs = reader.uint64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dispatchToken = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.errorType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.metadata = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionResult {
    return {
      actionId: isSet(object.actionId)
        ? globalThis.String(object.actionId)
        : isSet(object.action_id)
        ? globalThis.String(object.action_id)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      payload: isSet(object.payload) ? WorkflowArguments.fromJSON(object.payload) : undefined,
      workerStartNs: isSet(object.workerStartNs)
        ? BigInt(object.workerStartNs)
        : isSet(object.worker_start_ns)
        ? BigInt(object.worker_start_ns)
        : 0n,
      workerEndNs: isSet(object.workerEndNs)
        ? BigInt(object.workerEndNs)
        : isSet(object.worker_end_ns)
        ? BigInt(object.worker_end_ns)
        : 0n,
      dispatchToken: isSet(object.dispatchToken)
        ? globalThis.String(object.dispatchToken)
        : isSet(object.dispatch_token)
        ? globalThis.String(object.dispatch_token)
        : undefined,
      errorType: isSet(object.errorType)
        ? globalThis.String(object.errorType)
        : isSet(object.error_type)
        ? globalThis.String(object.error_type)
        : undefined,
      errorMessage: isSet(object.errorMessage)
        ? globalThis.String(object.errorMessage)
        : isSet(object.error_message)
        ? globalThis.String(object.error_message)
        : undefined,
      metadata: isSet(object.metadata) ? Buffer.from(bytesFromBase64(object.metadata)) : Buffer.alloc(0),
    };
  },

  toJSON(message: ActionResult): unknown {
    const obj: any = {};
    if (message.actionId !== "") {
      obj.actionId = message.actionId;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.payload !== undefined) {
      obj.payload = WorkflowArguments.toJSON(message.payload);
    }
    if (message.workerStartNs !== 0n) {
      obj.workerStartNs = message.workerStartNs.toString();
    }
    if (message.workerEndNs !== 0n) {
      obj.workerEndNs = message.workerEndNs.toString();
    }
    if (message.dispatchToken !== undefined) {
      obj.dispatchToken = message.dispatchToken;
    }
    if (message.errorType !== undefined) {
      obj.errorType = message.errorType;
    }
    if (message.errorMessage !== undefined) {
      obj.errorMessage = message.errorMessage;
    }
    if (message.metadata.length !== 0) {
      obj.metadata = base64FromBytes(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionResult>, I>>(base?: I): ActionResult {
    return ActionResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionResult>, I>>(object: I): ActionResult {
    const message = createBaseActionResult();
    message.actionId = object.actionId ?? "";
    message.success = object.success ?? false;
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? WorkflowArguments.fromPartial(object.payload)
      : undefined;
    message.workerStartNs = (object.workerStartNs !== undefined && object.workerStartNs !== null)
      ? BigInt(object.workerStartNs)
      : 0n;
    message.workerEndNs = (object.workerEndNs !== undefined && object.workerEndNs !== null)
      ? BigInt(object.workerEndNs)
      : 0n;
    message.dispatchToken = object.dispatchToken ?? undefined;
    message.errorType = object.errorType ?? undefined;
    message.errorMessage = object.errorMessage ?? undefined;
    message.metadata = object.metadata ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseAck(): Ack {
  return { ackedDeliveryId: 0n };
}

export const Ack: MessageFns<Ack> = {
  encode(message: Ack, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ackedDeliveryId !== 0n) {
      if (BigInt.asUintN(64, message.ackedDeliveryId) !== message.ackedDeliveryId) {
        throw new globalThis.Error("value provided for field message.ackedDeliveryId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.ackedDeliveryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ack {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ackedDeliveryId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Ack {
    return {
      ackedDeliveryId: isSet(object.ackedDeliveryId)
        ? BigInt(object.ackedDeliveryId)
        : isSet(object.acked_delivery_id)
        ? BigInt(object.acked_delivery_id)
        : 0n,
    };
  },

  toJSON(message: Ack): unknown {
    const obj: any = {};
    if (message.ackedDeliveryId !== 0n) {
      obj.ackedDeliveryId = message.ackedDeliveryId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Ack>, I>>(base?: I): Ack {
    return Ack.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Ack>, I>>(object: I): Ack {
    const message = createBaseAck();
    message.ackedDeliveryId = (object.ackedDeliveryId !== undefined && object.ackedDeliveryId !== null)
      ? BigInt(object.ackedDeliveryId)
      : 0n;
    return message;
  },
};

function createBaseWorkerHello(): WorkerHello {
  return { workerId: 0n, runtime: 0 };
}

export const WorkerHello: MessageFns<WorkerHello> = {
  encode(message: WorkerHello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workerId !== 0n) {
      if (BigInt.asUintN(64, message.workerId) !== message.workerId) {
        throw new globalThis.Error("value provided for field message.workerId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.workerId);
    }
    if (message.runtime !== 0) {
      writer.uint32(16).int32(message.runtime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkerHello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkerHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.workerId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.runtime = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkerHello {
    return {
      workerId: isSet(object.workerId)
        ? BigInt(object.workerId)
        : isSet(object.worker_id)
        ? BigInt(object.worker_id)
        : 0n,
      runtime: isSet(object.runtime) ? actionRuntimeFromJSON(object.runtime) : 0,
    };
  },

  toJSON(message: WorkerHello): unknown {
    const obj: any = {};
    if (message.workerId !== 0n) {
      obj.workerId = message.workerId.toString();
    }
    if (message.runtime !== 0) {
      obj.runtime = actionRuntimeToJSON(message.runtime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkerHello>, I>>(base?: I): WorkerHello {
    return WorkerHello.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkerHello>, I>>(object: I): WorkerHello {
    const message = createBaseWorkerHello();
    message.workerId = (object.workerId !== undefined && object.workerId !== null) ? BigInt(object.workerId) : 0n;
    message.runtime = object.runtime ?? 0;
    return message;
  },
};

function createBaseWorkflowArgumentValue(): WorkflowArgumentValue {
  return { kind: undefined };
}

export const WorkflowArgumentValue: MessageFns<WorkflowArgumentValue> = {
  encode(message: WorkflowArgumentValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "primitive":
        PrimitiveWorkflowArgument.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "basemodel":
        BaseModelWorkflowArgument.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
      case "exception":
        WorkflowErrorValue.encode(message.kind.value, writer.uint32(26).fork()).join();
        break;
      case "listValue":
        WorkflowListArgument.encode(message.kind.value, writer.uint32(34).fork()).join();
        break;
      case "tupleValue":
        WorkflowTupleArgument.encode(message.kind.value, writer.uint32(42).fork()).join();
        break;
      case "dictValue":
        WorkflowDictArgument.encode(message.kind.value, writer.uint32(50).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowArgumentValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowArgumentValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "primitive", value: PrimitiveWorkflowArgument.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "basemodel", value: BaseModelWorkflowArgument.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = { $case: "exception", value: WorkflowErrorValue.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kind = { $case: "listValue", value: WorkflowListArgument.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.kind = { $case: "tupleValue", value: WorkflowTupleArgument.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = { $case: "dictValue", value: WorkflowDictArgument.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowArgumentValue {
    return {
      kind: isSet(object.primitive)
        ? { $case: "primitive", value: PrimitiveWorkflowArgument.fromJSON(object.primitive) }
        : isSet(object.basemodel)
        ? { $case: "basemodel", value: BaseModelWorkflowArgument.fromJSON(object.basemodel) }
        : isSet(object.exception)
        ? { $case: "exception", value: WorkflowErrorValue.fromJSON(object.exception) }
        : isSet(object.listValue)
        ? { $case: "listValue", value: WorkflowListArgument.fromJSON(object.listValue) }
        : isSet(object.list_value)
        ? { $case: "listValue", value: WorkflowListArgument.fromJSON(object.list_value) }
        : isSet(object.tupleValue)
        ? { $case: "tupleValue", value: WorkflowTupleArgument.fromJSON(object.tupleValue) }
        : isSet(object.tuple_value)
        ? { $case: "tupleValue", value: WorkflowTupleArgument.fromJSON(object.tuple_value) }
        : isSet(object.dictValue)
        ? { $case: "dictValue", value: WorkflowDictArgument.fromJSON(object.dictValue) }
        : isSet(object.dict_value)
        ? { $case: "dictValue", value: WorkflowDictArgument.fromJSON(object.dict_value) }
        : undefined,
    };
  },

  toJSON(message: WorkflowArgumentValue): unknown {
    const obj: any = {};
    if (message.kind?.$case === "primitive") {
      obj.primitive = PrimitiveWorkflowArgument.toJSON(message.kind.value);
    } else if (message.kind?.$case === "basemodel") {
      obj.basemodel = BaseModelWorkflowArgument.toJSON(message.kind.value);
    } else if (message.kind?.$case === "exception") {
      obj.exception = WorkflowErrorValue.toJSON(message.kind.value);
    } else if (message.kind?.$case === "listValue") {
      obj.listValue = WorkflowListArgument.toJSON(message.kind.value);
    } else if (message.kind?.$case === "tupleValue") {
      obj.tupleValue = WorkflowTupleArgument.toJSON(message.kind.value);
    } else if (message.kind?.$case === "dictValue") {
      obj.dictValue = WorkflowDictArgument.toJSON(message.kind.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowArgumentValue>, I>>(base?: I): WorkflowArgumentValue {
    return WorkflowArgumentValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowArgumentValue>, I>>(object: I): WorkflowArgumentValue {
    const message = createBaseWorkflowArgumentValue();
    switch (object.kind?.$case) {
      case "primitive": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "primitive", value: PrimitiveWorkflowArgument.fromPartial(object.kind.value) };
        }
        break;
      }
      case "basemodel": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "basemodel", value: BaseModelWorkflowArgument.fromPartial(object.kind.value) };
        }
        break;
      }
      case "exception": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "exception", value: WorkflowErrorValue.fromPartial(object.kind.value) };
        }
        break;
      }
      case "listValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "listValue", value: WorkflowListArgument.fromPartial(object.kind.value) };
        }
        break;
      }
      case "tupleValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "tupleValue", value: WorkflowTupleArgument.fromPartial(object.kind.value) };
        }
        break;
      }
      case "dictValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "dictValue", value: WorkflowDictArgument.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseWorkflowArgument(): WorkflowArgument {
  return { key: "", value: undefined };
}

export const WorkflowArgument: MessageFns<WorkflowArgument> = {
  encode(message: WorkflowArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== undefined) {
      WorkflowArgumentValue.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = WorkflowArgumentValue.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowArgument {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? WorkflowArgumentValue.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: WorkflowArgument): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== undefined) {
      obj.value = WorkflowArgumentValue.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowArgument>, I>>(base?: I): WorkflowArgument {
    return WorkflowArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowArgument>, I>>(object: I): WorkflowArgument {
    const message = createBaseWorkflowArgument();
    message.key = object.key ?? "";
    message.value = (object.value !== undefined && object.value !== null)
      ? WorkflowArgumentValue.fromPartial(object.value)
      : undefined;
    return message;
  },
};

function createBaseWorkflowArguments(): WorkflowArguments {
  return { arguments: [] };
}

export const WorkflowArguments: MessageFns<WorkflowArguments> = {
  encode(message: WorkflowArguments, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.arguments) {
      WorkflowArgument.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowArguments {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowArguments();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.arguments.push(WorkflowArgument.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowArguments {
    return {
      arguments: globalThis.Array.isArray(object?.arguments)
        ? object.arguments.map((e: any) => WorkflowArgument.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowArguments): unknown {
    const obj: any = {};
    if (message.arguments?.length) {
      obj.arguments = message.arguments.map((e) => WorkflowArgument.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowArguments>, I>>(base?: I): WorkflowArguments {
    return WorkflowArguments.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowArguments>, I>>(object: I): WorkflowArguments {
    const message = createBaseWorkflowArguments();
    message.arguments = object.arguments?.map((e) => WorkflowArgument.fromPartial(e)) || [];
    return message;
  },
};

function createBasePrimitiveWorkflowArgument(): PrimitiveWorkflowArgument {
  return { kind: undefined };
}

export const PrimitiveWorkflowArgument: MessageFns<PrimitiveWorkflowArgument> = {
  encode(message: PrimitiveWorkflowArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "stringValue":
        writer.uint32(10).string(message.kind.value);
        break;
      case "doubleValue":
        writer.uint32(17).double(message.kind.value);
        break;
      case "intValue":
        if (BigInt.asIntN(64, message.kind.value) !== message.kind.value) {
          throw new globalThis.Error("value provided for field message.kind.value of type sint64 too large");
        }
        writer.uint32(24).sint64(message.kind.value);
        break;
      case "boolValue":
        writer.uint32(32).bool(message.kind.value);
        break;
      case "nullValue":
        writer.uint32(40).int32(message.kind.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrimitiveWorkflowArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrimitiveWorkflowArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "stringValue", value: reader.string() };
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.kind = { $case: "doubleValue", value: reader.double() };
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.kind = { $case: "intValue", value: reader.sint64() as bigint };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.kind = { $case: "boolValue", value: reader.bool() };
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.kind = { $case: "nullValue", value: reader.int32() as any };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrimitiveWorkflowArgument {
    return {
      kind: isSet(object.stringValue)
        ? { $case: "stringValue", value: globalThis.String(object.stringValue) }
        : isSet(object.string_value)
        ? { $case: "stringValue", value: globalThis.String(object.string_value) }
        : isSet(object.doubleValue)
        ? { $case: "doubleValue", value: globalThis.Number(object.doubleValue) }
        : isSet(object.double_value)
        ? { $case: "doubleValue", value: globalThis.Number(object.double_value) }
        : isSet(object.intValue)
        ? { $case: "intValue", value: BigInt(object.intValue) }
        : isSet(object.int_value)
        ? { $case: "intValue", value: BigInt(object.int_value) }
        : isSet(object.boolValue)
        ? { $case: "boolValue", value: globalThis.Boolean(object.boolValue) }
        : isSet(object.bool_value)
        ? { $case: "boolValue", value: globalThis.Boolean(object.bool_value) }
        : isSet(object.nullValue)
        ? { $case: "nullValue", value: nullValueFromJSON(object.nullValue) }
        : isSet(object.null_value)
        ? { $case: "nullValue", value: nullValueFromJSON(object.null_value) }
        : undefined,
    };
  },

  toJSON(message: PrimitiveWorkflowArgument): unknown {
    const obj: any = {};
    if (message.kind?.$case === "stringValue") {
      obj.stringValue = message.kind.value;
    } else if (message.kind?.$case === "doubleValue") {
      obj.doubleValue = message.kind.value;
    } else if (message.kind?.$case === "intValue") {
      obj.intValue = message.kind.value.toString();
    } else if (message.kind?.$case === "boolValue") {
      obj.boolValue = message.kind.value;
    } else if (message.kind?.$case === "nullValue") {
      obj.nullValue = nullValueToJSON(message.kind.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrimitiveWorkflowArgument>, I>>(base?: I): PrimitiveWorkflowArgument {
    return PrimitiveWorkflowArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrimitiveWorkflowArgument>, I>>(object: I): PrimitiveWorkflowArgument {
    const message = createBasePrimitiveWorkflowArgument();
    switch (object.kind?.$case) {
      case "stringValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "stringValue", value: object.kind.value };
        }
        break;
      }
      case "doubleValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "doubleValue", value: object.kind.value };
        }
        break;
      }
      case "intValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "intValue", value: BigInt(object.kind.value) };
        }
        break;
      }
      case "boolValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "boolValue", value: object.kind.value };
        }
        break;
      }
      case "nullValue": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "nullValue", value: object.kind.value };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseBaseModelWorkflowArgument(): BaseModelWorkflowArgument {
  return { module: "", name: "", data: undefined };
}

export const BaseModelWorkflowArgument: MessageFns<BaseModelWorkflowArgument> = {
  encode(message: BaseModelWorkflowArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.module !== "") {
      writer.uint32(10).string(message.module);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.data !== undefined) {
      WorkflowDictArgument.encode(message.data, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BaseModelWorkflowArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBaseModelWorkflowArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.module = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = WorkflowDictArgument.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BaseModelWorkflowArgument {
    return {
      module: isSet(object.module) ? globalThis.String(object.module) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      data: isSet(object.data) ? WorkflowDictArgument.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: BaseModelWorkflowArgument): unknown {
    const obj: any = {};
    if (message.module !== "") {
      obj.module = message.module;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.data !== undefined) {
      obj.data = WorkflowDictArgument.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BaseModelWorkflowArgument>, I>>(base?: I): BaseModelWorkflowArgument {
    return BaseModelWorkflowArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BaseModelWorkflowArgument>, I>>(object: I): BaseModelWorkflowArgument {
    const message = createBaseBaseModelWorkflowArgument();
    message.module = object.module ?? "";
    message.name = object.name ?? "";
    message.data = (object.data !== undefined && object.data !== null)
      ? WorkflowDictArgument.fromPartial(object.data)
      : undefined;
    return message;
  },
};

function createBaseWorkflowErrorValue(): WorkflowErrorValue {
  return { type: "", module: "", message: "", traceback: "", values: undefined, typeHierarchy: [] };
}

export const WorkflowErrorValue: MessageFns<WorkflowErrorValue> = {
  encode(message: WorkflowErrorValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.module !== "") {
      writer.uint32(18).string(message.module);
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.traceback !== "") {
      writer.uint32(34).string(message.traceback);
    }
    if (message.values !== undefined) {
      WorkflowDictArgument.encode(message.values, writer.uint32(42).fork()).join();
    }
    for (const v of message.typeHierarchy) {
      writer.uint32(50).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowErrorValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowErrorValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.module = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.traceback = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.values = WorkflowDictArgument.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.typeHierarchy.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowErrorValue {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      module: isSet(object.module) ? globalThis.String(object.module) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      traceback: isSet(object.traceback) ? globalThis.String(object.traceback) : "",
      values: isSet(object.values) ? WorkflowDictArgument.fromJSON(object.values) : undefined,
      typeHierarchy: globalThis.Array.isArray(object?.typeHierarchy)
        ? object.typeHierarchy.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.type_hierarchy)
        ? object.type_hierarchy.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: WorkflowErrorValue): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.module !== "") {
      obj.module = message.module;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.traceback !== "") {
      obj.traceback = message.traceback;
    }
    if (message.values !== undefined) {
      obj.values = WorkflowDictArgument.toJSON(message.values);
    }
    if (message.typeHierarchy?.length) {
      obj.typeHierarchy = message.typeHierarchy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowErrorValue>, I>>(base?: I): WorkflowErrorValue {
    return WorkflowErrorValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowErrorValue>, I>>(object: I): WorkflowErrorValue {
    const message = createBaseWorkflowErrorValue();
    message.type = object.type ?? "";
    message.module = object.module ?? "";
    message.message = object.message ?? "";
    message.traceback = object.traceback ?? "";
    message.values = (object.values !== undefined && object.values !== null)
      ? WorkflowDictArgument.fromPartial(object.values)
      : undefined;
    message.typeHierarchy = object.typeHierarchy?.map((e) => e) || [];
    return message;
  },
};

function createBaseWorkflowListArgument(): WorkflowListArgument {
  return { items: [] };
}

export const WorkflowListArgument: MessageFns<WorkflowListArgument> = {
  encode(message: WorkflowListArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      WorkflowArgumentValue.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowListArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowListArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(WorkflowArgumentValue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowListArgument {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => WorkflowArgumentValue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowListArgument): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => WorkflowArgumentValue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowListArgument>, I>>(base?: I): WorkflowListArgument {
    return WorkflowListArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowListArgument>, I>>(object: I): WorkflowListArgument {
    const message = createBaseWorkflowListArgument();
    message.items = object.items?.map((e) => WorkflowArgumentValue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowTupleArgument(): WorkflowTupleArgument {
  return { items: [] };
}

export const WorkflowTupleArgument: MessageFns<WorkflowTupleArgument> = {
  encode(message: WorkflowTupleArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      WorkflowArgumentValue.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowTupleArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowTupleArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(WorkflowArgumentValue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowTupleArgument {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => WorkflowArgumentValue.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowTupleArgument): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => WorkflowArgumentValue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowTupleArgument>, I>>(base?: I): WorkflowTupleArgument {
    return WorkflowTupleArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowTupleArgument>, I>>(object: I): WorkflowTupleArgument {
    const message = createBaseWorkflowTupleArgument();
    message.items = object.items?.map((e) => WorkflowArgumentValue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowDictArgument(): WorkflowDictArgument {
  return { entries: [] };
}

export const WorkflowDictArgument: MessageFns<WorkflowDictArgument> = {
  encode(message: WorkflowDictArgument, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      WorkflowArgument.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowDictArgument {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowDictArgument();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(WorkflowArgument.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowDictArgument {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => WorkflowArgument.fromJSON(e))
        : [],
    };
  },

  toJSON(message: WorkflowDictArgument): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => WorkflowArgument.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowDictArgument>, I>>(base?: I): WorkflowDictArgument {
    return WorkflowDictArgument.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowDictArgument>, I>>(object: I): WorkflowDictArgument {
    const message = createBaseWorkflowDictArgument();
    message.entries = object.entries?.map((e) => WorkflowArgument.fromPartial(e)) || [];
    return message;
  },
};

function createBaseWorkflowRegistration(): WorkflowRegistration {
  return {
    workflowName: "",
    ir: Buffer.alloc(0),
    irHash: "",
    workflowVersion: "",
    initialContext: undefined,
    concurrent: false,
    priority: undefined,
  };
}

export const WorkflowRegistration: MessageFns<WorkflowRegistration> = {
  encode(message: WorkflowRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.ir.length !== 0) {
      writer.uint32(18).bytes(message.ir);
    }
    if (message.irHash !== "") {
      writer.uint32(26).string(message.irHash);
    }
    if (message.workflowVersion !== "") {
      writer.uint32(58).string(message.workflowVersion);
    }
    if (message.initialContext !== undefined) {
      WorkflowArguments.encode(message.initialContext, writer.uint32(34).fork()).join();
    }
    if (message.concurrent !== false) {
      writer.uint32(40).bool(message.concurrent);
    }
    if (message.priority !== undefined) {
      writer.uint32(48).int32(message.priority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ir = Buffer.from(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.irHash = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.workflowVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.initialContext = WorkflowArguments.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.concurrent = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.priority = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowRegistration {
    return {
      workflowName: isSet(object.workflowName)
        ? globalThis.String(object.workflowName)
        : isSet(object.workflow_name)
        ? globalThis.String(object.workflow_name)
        : "",
      ir: isSet(object.ir) ? Buffer.from(bytesFromBase64(object.ir)) : Buffer.alloc(0),
      irHash: isSet(object.irHash)
        ? globalThis.String(object.irHash)
        : isSet(object.ir_hash)
        ? globalThis.String(object.ir_hash)
        : "",
      workflowVersion: isSet(object.workflowVersion)
        ? globalThis.String(object.workflowVersion)
        : isSet(object.workflow_version)
        ? globalThis.String(object.workflow_version)
        : "",
      initialContext: isSet(object.initialContext)
        ? WorkflowArguments.fromJSON(object.initialContext)
        : isSet(object.initial_context)
        ? WorkflowArguments.fromJSON(object.initial_context)
        : undefined,
      concurrent: isSet(object.concurrent) ? globalThis.Boolean(object.concurrent) : false,
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : undefined,
    };
  },

  toJSON(message: WorkflowRegistration): unknown {
    const obj: any = {};
    if (message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.ir.length !== 0) {
      obj.ir = base64FromBytes(message.ir);
    }
    if (message.irHash !== "") {
      obj.irHash = message.irHash;
    }
    if (message.workflowVersion !== "") {
      obj.workflowVersion = message.workflowVersion;
    }
    if (message.initialContext !== undefined) {
      obj.initialContext = WorkflowArguments.toJSON(message.initialContext);
    }
    if (message.concurrent !== false) {
      obj.concurrent = message.concurrent;
    }
    if (message.priority !== undefined) {
      obj.priority = Math.round(message.priority);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowRegistration>, I>>(base?: I): WorkflowRegistration {
    return WorkflowRegistration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowRegistration>, I>>(object: I): WorkflowRegistration {
    const message = createBaseWorkflowRegistration();
    message.workflowName = object.workflowName ?? "";
    message.ir = object.ir ?? Buffer.alloc(0);
    message.irHash = object.irHash ?? "";
    message.workflowVersion = object.workflowVersion ?? "";
    message.initialContext = (object.initialContext !== undefined && object.initialContext !== null)
      ? WorkflowArguments.fromPartial(object.initialContext)
      : undefined;
    message.concurrent = object.concurrent ?? false;
    message.priority = object.priority ?? undefined;
    return message;
  },
};

function createBaseRegisterWorkflowRequest(): RegisterWorkflowRequest {
  return { registration: undefined };
}

export const RegisterWorkflowRequest: MessageFns<RegisterWorkflowRequest> = {
  encode(message: RegisterWorkflowRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registration !== undefined) {
      WorkflowRegistration.encode(message.registration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkflowRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkflowRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registration = WorkflowRegistration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkflowRequest {
    return {
      registration: isSet(object.registration) ? WorkflowRegistration.fromJSON(object.registration) : undefined,
    };
  },

  toJSON(message: RegisterWorkflowRequest): unknown {
    const obj: any = {};
    if (message.registration !== undefined) {
      obj.registration = WorkflowRegistration.toJSON(message.registration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkflowRequest>, I>>(base?: I): RegisterWorkflowRequest {
    return RegisterWorkflowRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkflowRequest>, I>>(object: I): RegisterWorkflowRequest {
    const message = createBaseRegisterWorkflowRequest();
    message.registration = (object.registration !== undefined && object.registration !== null)
      ? WorkflowRegistration.fromPartial(object.registration)
      : undefined;
    return message;
  },
};

function createBaseRegisterWorkflowResponse(): RegisterWorkflowResponse {
  return { workflowVersionId: "", workflowInstanceId: "" };
}

export const RegisterWorkflowResponse: MessageFns<RegisterWorkflowResponse> = {
  encode(message: RegisterWorkflowResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowVersionId !== "") {
      writer.uint32(10).string(message.workflowVersionId);
    }
    if (message.workflowInstanceId !== "") {
      writer.uint32(18).string(message.workflowInstanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkflowResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkflowResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowVersionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workflowInstanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkflowResponse {
    return {
      workflowVersionId: isSet(object.workflowVersionId)
        ? globalThis.String(object.workflowVersionId)
        : isSet(object.workflow_version_id)
        ? globalThis.String(object.workflow_version_id)
        : "",
      workflowInstanceId: isSet(object.workflowInstanceId)
        ? globalThis.String(object.workflowInstanceId)
        : isSet(object.workflow_instance_id)
        ? globalThis.String(object.workflow_instance_id)
        : "",
    };
  },

  toJSON(message: RegisterWorkflowResponse): unknown {
    const obj: any = {};
    if (message.workflowVersionId !== "") {
      obj.workflowVersionId = message.workflowVersionId;
    }
    if (message.workflowInstanceId !== "") {
      obj.workflowInstanceId = message.workflowInstanceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkflowResponse>, I>>(base?: I): RegisterWorkflowResponse {
    return RegisterWorkflowResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkflowResponse>, I>>(object: I): RegisterWorkflowResponse {
    const message = createBaseRegisterWorkflowResponse();
    message.workflowVersionId = object.workflowVersionId ?? "";
    message.workflowInstanceId = object.workflowInstanceId ?? "";
    return message;
  },
};

function createBaseRegisterWorkflowBatchRequest(): RegisterWorkflowBatchRequest {
  return {
    registration: undefined,
    count: 0,
    inputs: undefined,
    inputsList: [],
    batchSize: 0,
    includeInstanceIds: false,
  };
}

export const RegisterWorkflowBatchRequest: MessageFns<RegisterWorkflowBatchRequest> = {
  encode(message: RegisterWorkflowBatchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registration !== undefined) {
      WorkflowRegistration.encode(message.registration, writer.uint32(10).fork()).join();
    }
    if (message.count !== 0) {
      writer.uint32(16).uint32(message.count);
    }
    if (message.inputs !== undefined) {
      WorkflowArguments.encode(message.inputs, writer.uint32(26).fork()).join();
    }
    for (const v of message.inputsList) {
      WorkflowArguments.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.batchSize !== 0) {
      writer.uint32(40).uint32(message.batchSize);
    }
    if (message.includeInstanceIds !== false) {
      writer.uint32(48).bool(message.includeInstanceIds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkflowBatchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkflowBatchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registration = WorkflowRegistration.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputs = WorkflowArguments.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.inputsList.push(WorkflowArguments.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.batchSize = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.includeInstanceIds = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkflowBatchRequest {
    return {
      registration: isSet(object.registration) ? WorkflowRegistration.fromJSON(object.registration) : undefined,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      inputs: isSet(object.inputs) ? WorkflowArguments.fromJSON(object.inputs) : undefined,
      inputsList: globalThis.Array.isArray(object?.inputsList)
        ? object.inputsList.map((e: any) => WorkflowArguments.fromJSON(e))
        : globalThis.Array.isArray(object?.inputs_list)
        ? object.inputs_list.map((e: any) => WorkflowArguments.fromJSON(e))
        : [],
      batchSize: isSet(object.batchSize)
        ? globalThis.Number(object.batchSize)
        : isSet(object.batch_size)
        ? globalThis.Number(object.batch_size)
        : 0,
      includeInstanceIds: isSet(object.includeInstanceIds)
        ? globalThis.Boolean(object.includeInstanceIds)
        : isSet(object.include_instance_ids)
        ? globalThis.Boolean(object.include_instance_ids)
        : false,
    };
  },

  toJSON(message: RegisterWorkflowBatchRequest): unknown {
    const obj: any = {};
    if (message.registration !== undefined) {
      obj.registration = WorkflowRegistration.toJSON(message.registration);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.inputs !== undefined) {
      obj.inputs = WorkflowArguments.toJSON(message.inputs);
    }
    if (message.inputsList?.length) {
      obj.inputsList = message.inputsList.map((e) => WorkflowArguments.toJSON(e));
    }
    if (message.batchSize !== 0) {
      obj.batchSize = Math.round(message.batchSize);
    }
    if (message.includeInstanceIds !== false) {
      obj.includeInstanceIds = message.includeInstanceIds;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkflowBatchRequest>, I>>(base?: I): RegisterWorkflowBatchRequest {
    return RegisterWorkflowBatchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkflowBatchRequest>, I>>(object: I): RegisterWorkflowBatchRequest {
    const message = createBaseRegisterWorkflowBatchRequest();
    message.registration = (object.registration !== undefined && object.registration !== null)
      ? WorkflowRegistration.fromPartial(object.registration)
      : undefined;
    message.count = object.count ?? 0;
    message.inputs = (object.inputs !== undefined && object.inputs !== null)
      ? WorkflowArguments.fromPartial(object.inputs)
      : undefined;
    message.inputsList = object.inputsList?.map((e) => WorkflowArguments.fromPartial(e)) || [];
    message.batchSize = object.batchSize ?? 0;
    message.includeInstanceIds = object.includeInstanceIds ?? false;
    return message;
  },
};

function createBaseRegisterWorkflowBatchResponse(): RegisterWorkflowBatchResponse {
  return { workflowVersionId: "", workflowInstanceIds: [], queued: 0 };
}

export const RegisterWorkflowBatchResponse: MessageFns<RegisterWorkflowBatchResponse> = {
  encode(message: RegisterWorkflowBatchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowVersionId !== "") {
      writer.uint32(10).string(message.workflowVersionId);
    }
    for (const v of message.workflowInstanceIds) {
      writer.uint32(18).string(v!);
    }
    if (message.queued !== 0) {
      writer.uint32(24).uint32(message.queued);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterWorkflowBatchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterWorkflowBatchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowVersionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workflowInstanceIds.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.queued = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterWorkflowBatchResponse {
    return {
      workflowVersionId: isSet(object.workflowVersionId)
        ? globalThis.String(object.workflowVersionId)
        : isSet(object.workflow_version_id)
        ? globalThis.String(object.workflow_version_id)
        : "",
      workflowInstanceIds: globalThis.Array.isArray(object?.workflowInstanceIds)
        ? object.workflowInstanceIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.workflow_instance_ids)
        ? object.workflow_instance_ids.map((e: any) => globalThis.String(e))
        : [],
      queued: isSet(object.queued) ? globalThis.Number(object.queued) : 0,
    };
  },

  toJSON(message: RegisterWorkflowBatchResponse): unknown {
    const obj: any = {};
    if (message.workflowVersionId !== "") {
      obj.workflowVersionId = message.workflowVersionId;
    }
    if (message.workflowInstanceIds?.length) {
      obj.workflowInstanceIds = message.workflowInstanceIds;
    }
    if (message.queued !== 0) {
      obj.queued = Math.round(message.queued);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterWorkflowBatchResponse>, I>>(base?: I): RegisterWorkflowBatchResponse {
    return RegisterWorkflowBatchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterWorkflowBatchResponse>, I>>(
    object: I,
  ): RegisterWorkflowBatchResponse {
    const message = createBaseRegisterWorkflowBatchResponse();
    message.workflowVersionId = object.workflowVersionId ?? "";
    message.workflowInstanceIds = object.workflowInstanceIds?.map((e) => e) || [];
    message.queued = object.queued ?? 0;
    return message;
  },
};

function createBaseWaitForInstanceRequest(): WaitForInstanceRequest {
  return { instanceId: "", pollIntervalSecs: 0 };
}

export const WaitForInstanceRequest: MessageFns<WaitForInstanceRequest> = {
  encode(message: WaitForInstanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instanceId !== "") {
      writer.uint32(10).string(message.instanceId);
    }
    if (message.pollIntervalSecs !== 0) {
      writer.uint32(17).double(message.pollIntervalSecs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitForInstanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitForInstanceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.instanceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.pollIntervalSecs = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitForInstanceRequest {
    return {
      instanceId: isSet(object.instanceId)
        ? globalThis.String(object.instanceId)
        : isSet(object.instance_id)
        ? globalThis.String(object.instance_id)
        : "",
      pollIntervalSecs: isSet(object.pollIntervalSecs)
        ? globalThis.Number(object.pollIntervalSecs)
        : isSet(object.poll_interval_secs)
        ? globalThis.Number(object.poll_interval_secs)
        : 0,
    };
  },

  toJSON(message: WaitForInstanceRequest): unknown {
    const obj: any = {};
    if (message.instanceId !== "") {
      obj.instanceId = message.instanceId;
    }
    if (message.pollIntervalSecs !== 0) {
      obj.pollIntervalSecs = message.pollIntervalSecs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaitForInstanceRequest>, I>>(base?: I): WaitForInstanceRequest {
    return WaitForInstanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaitForInstanceRequest>, I>>(object: I): WaitForInstanceRequest {
    const message = createBaseWaitForInstanceRequest();
    message.instanceId = object.instanceId ?? "";
    message.pollIntervalSecs = object.pollIntervalSecs ?? 0;
    return message;
  },
};

function createBaseWaitForInstanceResponse(): WaitForInstanceResponse {
  return { payload: Buffer.alloc(0) };
}

export const WaitForInstanceResponse: MessageFns<WaitForInstanceResponse> = {
  encode(message: WaitForInstanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WaitForInstanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWaitForInstanceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WaitForInstanceResponse {
    return { payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0) };
  },

  toJSON(message: WaitForInstanceResponse): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WaitForInstanceResponse>, I>>(base?: I): WaitForInstanceResponse {
    return WaitForInstanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WaitForInstanceResponse>, I>>(object: I): WaitForInstanceResponse {
    const message = createBaseWaitForInstanceResponse();
    message.payload = object.payload ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWorkflowStreamRequest(): WorkflowStreamRequest {
  return { kind: undefined, skipSleep: false };
}

export const WorkflowStreamRequest: MessageFns<WorkflowStreamRequest> = {
  encode(message: WorkflowStreamRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "registration":
        WorkflowRegistration.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "actionResult":
        ActionResult.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
    }
    if (message.skipSleep !== false) {
      writer.uint32(24).bool(message.skipSleep);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowStreamRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowStreamRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "registration", value: WorkflowRegistration.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "actionResult", value: ActionResult.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.skipSleep = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowStreamRequest {
    return {
      kind: isSet(object.registration)
        ? { $case: "registration", value: WorkflowRegistration.fromJSON(object.registration) }
        : isSet(object.actionResult)
        ? { $case: "actionResult", value: ActionResult.fromJSON(object.actionResult) }
        : isSet(object.action_result)
        ? { $case: "actionResult", value: ActionResult.fromJSON(object.action_result) }
        : undefined,
      skipSleep: isSet(object.skipSleep)
        ? globalThis.Boolean(object.skipSleep)
        : isSet(object.skip_sleep)
        ? globalThis.Boolean(object.skip_sleep)
        : false,
    };
  },

  toJSON(message: WorkflowStreamRequest): unknown {
    const obj: any = {};
    if (message.kind?.$case === "registration") {
      obj.registration = WorkflowRegistration.toJSON(message.kind.value);
    } else if (message.kind?.$case === "actionResult") {
      obj.actionResult = ActionResult.toJSON(message.kind.value);
    }
    if (message.skipSleep !== false) {
      obj.skipSleep = message.skipSleep;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowStreamRequest>, I>>(base?: I): WorkflowStreamRequest {
    return WorkflowStreamRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowStreamRequest>, I>>(object: I): WorkflowStreamRequest {
    const message = createBaseWorkflowStreamRequest();
    switch (object.kind?.$case) {
      case "registration": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "registration", value: WorkflowRegistration.fromPartial(object.kind.value) };
        }
        break;
      }
      case "actionResult": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "actionResult", value: ActionResult.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    message.skipSleep = object.skipSleep ?? false;
    return message;
  },
};

function createBaseWorkflowExecutionResult(): WorkflowExecutionResult {
  return { payload: Buffer.alloc(0) };
}

export const WorkflowExecutionResult: MessageFns<WorkflowExecutionResult> = {
  encode(message: WorkflowExecutionResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowExecutionResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowExecutionResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowExecutionResult {
    return { payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0) };
  },

  toJSON(message: WorkflowExecutionResult): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowExecutionResult>, I>>(base?: I): WorkflowExecutionResult {
    return WorkflowExecutionResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowExecutionResult>, I>>(object: I): WorkflowExecutionResult {
    const message = createBaseWorkflowExecutionResult();
    message.payload = object.payload ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseWorkflowStreamResponse(): WorkflowStreamResponse {
  return { kind: undefined };
}

export const WorkflowStreamResponse: MessageFns<WorkflowStreamResponse> = {
  encode(message: WorkflowStreamResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "actionDispatch":
        ActionDispatch.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "workflowResult":
        WorkflowExecutionResult.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkflowStreamResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkflowStreamResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "actionDispatch", value: ActionDispatch.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "workflowResult", value: WorkflowExecutionResult.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkflowStreamResponse {
    return {
      kind: isSet(object.actionDispatch)
        ? { $case: "actionDispatch", value: ActionDispatch.fromJSON(object.actionDispatch) }
        : isSet(object.action_dispatch)
        ? { $case: "actionDispatch", value: ActionDispatch.fromJSON(object.action_dispatch) }
        : isSet(object.workflowResult)
        ? { $case: "workflowResult", value: WorkflowExecutionResult.fromJSON(object.workflowResult) }
        : isSet(object.workflow_result)
        ? { $case: "workflowResult", value: WorkflowExecutionResult.fromJSON(object.workflow_result) }
        : undefined,
    };
  },

  toJSON(message: WorkflowStreamResponse): unknown {
    const obj: any = {};
    if (message.kind?.$case === "actionDispatch") {
      obj.actionDispatch = ActionDispatch.toJSON(message.kind.value);
    } else if (message.kind?.$case === "workflowResult") {
      obj.workflowResult = WorkflowExecutionResult.toJSON(message.kind.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WorkflowStreamResponse>, I>>(base?: I): WorkflowStreamResponse {
    return WorkflowStreamResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WorkflowStreamResponse>, I>>(object: I): WorkflowStreamResponse {
    const message = createBaseWorkflowStreamResponse();
    switch (object.kind?.$case) {
      case "actionDispatch": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "actionDispatch", value: ActionDispatch.fromPartial(object.kind.value) };
        }
        break;
      }
      case "workflowResult": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "workflowResult", value: WorkflowExecutionResult.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseScheduleDefinition(): ScheduleDefinition {
  return { type: 0, cronExpression: "", intervalSeconds: 0n, jitterSeconds: 0n };
}

export const ScheduleDefinition: MessageFns<ScheduleDefinition> = {
  encode(message: ScheduleDefinition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).int32(message.type);
    }
    if (message.cronExpression !== "") {
      writer.uint32(18).string(message.cronExpression);
    }
    if (message.intervalSeconds !== 0n) {
      if (BigInt.asIntN(64, message.intervalSeconds) !== message.intervalSeconds) {
        throw new globalThis.Error("value provided for field message.intervalSeconds of type int64 too large");
      }
      writer.uint32(24).int64(message.intervalSeconds);
    }
    if (message.jitterSeconds !== 0n) {
      if (BigInt.asIntN(64, message.jitterSeconds) !== message.jitterSeconds) {
        throw new globalThis.Error("value provided for field message.jitterSeconds of type int64 too large");
      }
      writer.uint32(32).int64(message.jitterSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleDefinition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleDefinition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cronExpression = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.intervalSeconds = reader.int64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.jitterSeconds = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleDefinition {
    return {
      type: isSet(object.type) ? scheduleTypeFromJSON(object.type) : 0,
      cronExpression: isSet(object.cronExpression)
        ? globalThis.String(object.cronExpression)
        : isSet(object.cron_expression)
        ? globalThis.String(object.cron_expression)
        : "",
      intervalSeconds: isSet(object.intervalSeconds)
        ? BigInt(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? BigInt(object.interval_seconds)
        : 0n,
      jitterSeconds: isSet(object.jitterSeconds)
        ? BigInt(object.jitterSeconds)
        : isSet(object.jitter_seconds)
        ? BigInt(object.jitter_seconds)
        : 0n,
    };
  },

  toJSON(message: ScheduleDefinition): unknown {
    const obj: any = {};
    if (message.type !== 0) {
      obj.type = scheduleTypeToJSON(message.type);
    }
    if (message.cronExpression !== "") {
      obj.cronExpression = message.cronExpression;
    }
    if (message.intervalSeconds !== 0n) {
      obj.intervalSeconds = message.intervalSeconds.toString();
    }
    if (message.jitterSeconds !== 0n) {
      obj.jitterSeconds = message.jitterSeconds.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduleDefinition>, I>>(base?: I): ScheduleDefinition {
    return ScheduleDefinition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduleDefinition>, I>>(object: I): ScheduleDefinition {
    const message = createBaseScheduleDefinition();
    message.type = object.type ?? 0;
    message.cronExpression = object.cronExpression ?? "";
    message.intervalSeconds = (object.intervalSeconds !== undefined && object.intervalSeconds !== null)
      ? BigInt(object.intervalSeconds)
      : 0n;
    message.jitterSeconds = (object.jitterSeconds !== undefined && object.jitterSeconds !== null)
      ? BigInt(object.jitterSeconds)
      : 0n;
    return message;
  },
};

function createBaseRegisterScheduleRequest(): RegisterScheduleRequest {
  return {
    workflowName: "",
    schedule: undefined,
    inputs: undefined,
    registration: undefined,
    scheduleName: "",
    priority: undefined,
    allowDuplicate: undefined,
  };
}

export const RegisterScheduleRequest: MessageFns<RegisterScheduleRequest> = {
  encode(message: RegisterScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.schedule !== undefined) {
      ScheduleDefinition.encode(message.schedule, writer.uint32(18).fork()).join();
    }
    if (message.inputs !== undefined) {
      WorkflowArguments.encode(message.inputs, writer.uint32(26).fork()).join();
    }
    if (message.registration !== undefined) {
      WorkflowRegistration.encode(message.registration, writer.uint32(34).fork()).join();
    }
    if (message.scheduleName !== "") {
      writer.uint32(42).string(message.scheduleName);
    }
    if (message.priority !== undefined) {
      writer.uint32(48).int32(message.priority);
    }
    if (message.allowDuplicate !== undefined) {
      writer.uint32(56).bool(message.allowDuplicate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.schedule = ScheduleDefinition.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inputs = WorkflowArguments.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.registration = WorkflowRegistration.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.scheduleName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.priority = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.allowDuplicate = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterScheduleRequest {
    return {
      workflowName: isSet(object.workflowName)
        ? globalThis.String(object.workflowName)
        : isSet(object.workflow_name)
        ? globalThis.String(object.workflow_name)
        : "",
      schedule: isSet(object.schedule) ? ScheduleDefinition.fromJSON(object.schedule) : undefined,
      inputs: isSet(object.inputs) ? WorkflowArguments.fromJSON(object.inputs) : undefined,
      registration: isSet(object.registration) ? WorkflowRegistration.fromJSON(object.registration) : undefined,
      scheduleName: isSet(object.scheduleName)
        ? globalThis.String(object.scheduleName)
        : isSet(object.schedule_name)
        ? globalThis.String(object.schedule_name)
        : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : undefined,
      allowDuplicate: isSet(object.allowDuplicate)
        ? globalThis.Boolean(object.allowDuplicate)
        : isSet(object.allow_duplicate)
        ? globalThis.Boolean(object.allow_duplicate)
        : undefined,
    };
  },

  toJSON(message: RegisterScheduleRequest): unknown {
    const obj: any = {};
    if (message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.schedule !== undefined) {
      obj.schedule = ScheduleDefinition.toJSON(message.schedule);
    }
    if (message.inputs !== undefined) {
      obj.inputs = WorkflowArguments.toJSON(message.inputs);
    }
    if (message.registration !== undefined) {
      obj.registration = WorkflowRegistration.toJSON(message.registration);
    }
    if (message.scheduleName !== "") {
      obj.scheduleName = message.scheduleName;
    }
    if (message.priority !== undefined) {
      obj.priority = Math.round(message.priority);
    }
    if (message.allowDuplicate !== undefined) {
      obj.allowDuplicate = message.allowDuplicate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterScheduleRequest>, I>>(base?: I): RegisterScheduleRequest {
    return RegisterScheduleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterScheduleRequest>, I>>(object: I): RegisterScheduleRequest {
    const message = createBaseRegisterScheduleRequest();
    message.workflowName = object.workflowName ?? "";
    message.schedule = (object.schedule !== undefined && object.schedule !== null)
      ? ScheduleDefinition.fromPartial(object.schedule)
      : undefined;
    message.inputs = (object.inputs !== undefined && object.inputs !== null)
      ? WorkflowArguments.fromPartial(object.inputs)
      : undefined;
    message.registration = (object.registration !== undefined && object.registration !== null)
      ? WorkflowRegistration.fromPartial(object.registration)
      : undefined;
    message.scheduleName = object.scheduleName ?? "";
    message.priority = object.priority ?? undefined;
    message.allowDuplicate = object.allowDuplicate ?? undefined;
    return message;
  },
};

function createBaseRegisterScheduleResponse(): RegisterScheduleResponse {
  return { scheduleId: "", nextRunAt: "" };
}

export const RegisterScheduleResponse: MessageFns<RegisterScheduleResponse> = {
  encode(message: RegisterScheduleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scheduleId !== "") {
      writer.uint32(10).string(message.scheduleId);
    }
    if (message.nextRunAt !== "") {
      writer.uint32(18).string(message.nextRunAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterScheduleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterScheduleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nextRunAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterScheduleResponse {
    return {
      scheduleId: isSet(object.scheduleId)
        ? globalThis.String(object.scheduleId)
        : isSet(object.schedule_id)
        ? globalThis.String(object.schedule_id)
        : "",
      nextRunAt: isSet(object.nextRunAt)
        ? globalThis.String(object.nextRunAt)
        : isSet(object.next_run_at)
        ? globalThis.String(object.next_run_at)
        : "",
    };
  },

  toJSON(message: RegisterScheduleResponse): unknown {
    const obj: any = {};
    if (message.scheduleId !== "") {
      obj.scheduleId = message.scheduleId;
    }
    if (message.nextRunAt !== "") {
      obj.nextRunAt = message.nextRunAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterScheduleResponse>, I>>(base?: I): RegisterScheduleResponse {
    return RegisterScheduleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterScheduleResponse>, I>>(object: I): RegisterScheduleResponse {
    const message = createBaseRegisterScheduleResponse();
    message.scheduleId = object.scheduleId ?? "";
    message.nextRunAt = object.nextRunAt ?? "";
    return message;
  },
};

function createBaseUpdateScheduleStatusRequest(): UpdateScheduleStatusRequest {
  return { workflowName: "", status: 0, scheduleName: "" };
}

export const UpdateScheduleStatusRequest: MessageFns<UpdateScheduleStatusRequest> = {
  encode(message: UpdateScheduleStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    if (message.scheduleName !== "") {
      writer.uint32(26).string(message.scheduleName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateScheduleStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateScheduleStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheduleName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateScheduleStatusRequest {
    return {
      workflowName: isSet(object.workflowName)
        ? globalThis.String(object.workflowName)
        : isSet(object.workflow_name)
        ? globalThis.String(object.workflow_name)
        : "",
      status: isSet(object.status) ? scheduleStatusFromJSON(object.status) : 0,
      scheduleName: isSet(object.scheduleName)
        ? globalThis.String(object.scheduleName)
        : isSet(object.schedule_name)
        ? globalThis.String(object.schedule_name)
        : "",
    };
  },

  toJSON(message: UpdateScheduleStatusRequest): unknown {
    const obj: any = {};
    if (message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.status !== 0) {
      obj.status = scheduleStatusToJSON(message.status);
    }
    if (message.scheduleName !== "") {
      obj.scheduleName = message.scheduleName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateScheduleStatusRequest>, I>>(base?: I): UpdateScheduleStatusRequest {
    return UpdateScheduleStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateScheduleStatusRequest>, I>>(object: I): UpdateScheduleStatusRequest {
    const message = createBaseUpdateScheduleStatusRequest();
    message.workflowName = object.workflowName ?? "";
    message.status = object.status ?? 0;
    message.scheduleName = object.scheduleName ?? "";
    return message;
  },
};

function createBaseUpdateScheduleStatusResponse(): UpdateScheduleStatusResponse {
  return { success: false };
}

export const UpdateScheduleStatusResponse: MessageFns<UpdateScheduleStatusResponse> = {
  encode(message: UpdateScheduleStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateScheduleStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateScheduleStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateScheduleStatusResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: UpdateScheduleStatusResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateScheduleStatusResponse>, I>>(base?: I): UpdateScheduleStatusResponse {
    return UpdateScheduleStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateScheduleStatusResponse>, I>>(object: I): UpdateScheduleStatusResponse {
    const message = createBaseUpdateScheduleStatusResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseDeleteScheduleRequest(): DeleteScheduleRequest {
  return { workflowName: "", scheduleName: "" };
}

export const DeleteScheduleRequest: MessageFns<DeleteScheduleRequest> = {
  encode(message: DeleteScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workflowName !== "") {
      writer.uint32(10).string(message.workflowName);
    }
    if (message.scheduleName !== "") {
      writer.uint32(18).string(message.scheduleName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.scheduleName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteScheduleRequest {
    return {
      workflowName: isSet(object.workflowName)
        ? globalThis.String(object.workflowName)
        : isSet(object.workflow_name)
        ? globalThis.String(object.workflow_name)
        : "",
      scheduleName: isSet(object.scheduleName)
        ? globalThis.String(object.scheduleName)
        : isSet(object.schedule_name)
        ? globalThis.String(object.schedule_name)
        : "",
    };
  },

  toJSON(message: DeleteScheduleRequest): unknown {
    const obj: any = {};
    if (message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.scheduleName !== "") {
      obj.scheduleName = message.scheduleName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteScheduleRequest>, I>>(base?: I): DeleteScheduleRequest {
    return DeleteScheduleRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteScheduleRequest>, I>>(object: I): DeleteScheduleRequest {
    const message = createBaseDeleteScheduleRequest();
    message.workflowName = object.workflowName ?? "";
    message.scheduleName = object.scheduleName ?? "";
    return message;
  },
};

function createBaseDeleteScheduleResponse(): DeleteScheduleResponse {
  return { success: false };
}

export const DeleteScheduleResponse: MessageFns<DeleteScheduleResponse> = {
  encode(message: DeleteScheduleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteScheduleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteScheduleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteScheduleResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DeleteScheduleResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteScheduleResponse>, I>>(base?: I): DeleteScheduleResponse {
    return DeleteScheduleResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteScheduleResponse>, I>>(object: I): DeleteScheduleResponse {
    const message = createBaseDeleteScheduleResponse();
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseListSchedulesRequest(): ListSchedulesRequest {
  return { statusFilter: undefined };
}

export const ListSchedulesRequest: MessageFns<ListSchedulesRequest> = {
  encode(message: ListSchedulesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.statusFilter !== undefined) {
      writer.uint32(10).string(message.statusFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.statusFilter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSchedulesRequest {
    return {
      statusFilter: isSet(object.statusFilter)
        ? globalThis.String(object.statusFilter)
        : isSet(object.status_filter)
        ? globalThis.String(object.status_filter)
        : undefined,
    };
  },

  toJSON(message: ListSchedulesRequest): unknown {
    const obj: any = {};
    if (message.statusFilter !== undefined) {
      obj.statusFilter = message.statusFilter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSchedulesRequest>, I>>(base?: I): ListSchedulesRequest {
    return ListSchedulesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSchedulesRequest>, I>>(object: I): ListSchedulesRequest {
    const message = createBaseListSchedulesRequest();
    message.statusFilter = object.statusFilter ?? undefined;
    return message;
  },
};

function createBaseScheduleInfo(): ScheduleInfo {
  return {
    id: "",
    workflowName: "",
    scheduleType: 0,
    cronExpression: "",
    intervalSeconds: 0n,
    status: 0,
    nextRunAt: "",
    lastRunAt: "",
    lastInstanceId: "",
    createdAt: "",
    updatedAt: "",
    scheduleName: "",
    jitterSeconds: 0n,
    allowDuplicate: false,
  };
}

export const ScheduleInfo: MessageFns<ScheduleInfo> = {
  encode(message: ScheduleInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.workflowName !== "") {
      writer.uint32(18).string(message.workflowName);
    }
    if (message.scheduleType !== 0) {
      writer.uint32(24).int32(message.scheduleType);
    }
    if (message.cronExpression !== "") {
      writer.uint32(34).string(message.cronExpression);
    }
    if (message.intervalSeconds !== 0n) {
      if (BigInt.asIntN(64, message.intervalSeconds) !== message.intervalSeconds) {
        throw new globalThis.Error("value provided for field message.intervalSeconds of type int64 too large");
      }
      writer.uint32(40).int64(message.intervalSeconds);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    if (message.nextRunAt !== "") {
      writer.uint32(58).string(message.nextRunAt);
    }
    if (message.lastRunAt !== "") {
      writer.uint32(66).string(message.lastRunAt);
    }
    if (message.lastInstanceId !== "") {
      writer.uint32(74).string(message.lastInstanceId);
    }
    if (message.createdAt !== "") {
      writer.uint32(82).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(90).string(message.updatedAt);
    }
    if (message.scheduleName !== "") {
      writer.uint32(98).string(message.scheduleName);
    }
    if (message.jitterSeconds !== 0n) {
      if (BigInt.asIntN(64, message.jitterSeconds) !== message.jitterSeconds) {
        throw new globalThis.Error("value provided for field message.jitterSeconds of type int64 too large");
      }
      writer.uint32(104).int64(message.jitterSeconds);
    }
    if (message.allowDuplicate !== false) {
      writer.uint32(112).bool(message.allowDuplicate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduleInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduleInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workflowName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.scheduleType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cronExpression = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.intervalSeconds = reader.int64() as bigint;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.nextRunAt = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastRunAt = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastInstanceId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.scheduleName = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.jitterSeconds = reader.int64() as bigint;
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.allowDuplicate = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduleInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      workflowName: isSet(object.workflowName)
        ? globalThis.String(object.workflowName)
        : isSet(object.workflow_name)
        ? globalThis.String(object.workflow_name)
        : "",
      scheduleType: isSet(object.scheduleType)
        ? scheduleTypeFromJSON(object.scheduleType)
        : isSet(object.schedule_type)
        ? scheduleTypeFromJSON(object.schedule_type)
        : 0,
      cronExpression: isSet(object.cronExpression)
        ? globalThis.String(object.cronExpression)
        : isSet(object.cron_expression)
        ? globalThis.String(object.cron_expression)
        : "",
      intervalSeconds: isSet(object.intervalSeconds)
        ? BigInt(object.intervalSeconds)
        : isSet(object.interval_seconds)
        ? BigInt(object.interval_seconds)
        : 0n,
      status: isSet(object.status) ? scheduleStatusFromJSON(object.status) : 0,
      nextRunAt: isSet(object.nextRunAt)
        ? globalThis.String(object.nextRunAt)
        : isSet(object.next_run_at)
        ? globalThis.String(object.next_run_at)
        : "",
      lastRunAt: isSet(object.lastRunAt)
        ? globalThis.String(object.lastRunAt)
        : isSet(object.last_run_at)
        ? globalThis.String(object.last_run_at)
        : "",
      lastInstanceId: isSet(object.lastInstanceId)
        ? globalThis.String(object.lastInstanceId)
        : isSet(object.last_instance_id)
        ? globalThis.String(object.last_instance_id)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      scheduleName: isSet(object.scheduleName)
        ? globalThis.String(object.scheduleName)
        : isSet(object.schedule_name)
        ? globalThis.String(object.schedule_name)
        : "",
      jitterSeconds: isSet(object.jitterSeconds)
        ? BigInt(object.jitterSeconds)
        : isSet(object.jitter_seconds)
        ? BigInt(object.jitter_seconds)
        : 0n,
      allowDuplicate: isSet(object.allowDuplicate)
        ? globalThis.Boolean(object.allowDuplicate)
        : isSet(object.allow_duplicate)
        ? globalThis.Boolean(object.allow_duplicate)
        : false,
    };
  },

  toJSON(message: ScheduleInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.workflowName !== "") {
      obj.workflowName = message.workflowName;
    }
    if (message.scheduleType !== 0) {
      obj.scheduleType = scheduleTypeToJSON(message.scheduleType);
    }
    if (message.cronExpression !== "") {
      obj.cronExpression = message.cronExpression;
    }
    if (message.intervalSeconds !== 0n) {
      obj.intervalSeconds = message.intervalSeconds.toString();
    }
    if (message.status !== 0) {
      obj.status = scheduleStatusToJSON(message.status);
    }
    if (message.nextRunAt !== "") {
      obj.nextRunAt = message.nextRunAt;
    }
    if (message.lastRunAt !== "") {
      obj.lastRunAt = message.lastRunAt;
    }
    if (message.lastInstanceId !== "") {
      obj.lastInstanceId = message.lastInstanceId;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.scheduleName !== "") {
      obj.scheduleName = message.scheduleName;
    }
    if (message.jitterSeconds !== 0n) {
      obj.jitterSeconds = message.jitterSeconds.toString();
    }
    if (message.allowDuplicate !== false) {
      obj.allowDuplicate = message.allowDuplicate;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScheduleInfo>, I>>(base?: I): ScheduleInfo {
    return ScheduleInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScheduleInfo>, I>>(object: I): ScheduleInfo {
    const message = createBaseScheduleInfo();
    message.id = object.id ?? "";
    message.workflowName = object.workflowName ?? "";
    message.scheduleType = object.scheduleType ?? 0;
    message.cronExpression = object.cronExpression ?? "";
    message.intervalSeconds = (object.intervalSeconds !== undefined && object.intervalSeconds !== null)
      ? BigInt(object.intervalSeconds)
      : 0n;
    message.status = object.status ?? 0;
    message.nextRunAt = object.nextRunAt ?? "";
    message.lastRunAt = object.lastRunAt ?? "";
    message.lastInstanceId = object.lastInstanceId ?? "";
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.scheduleName = object.scheduleName ?? "";
    message.jitterSeconds = (object.jitterSeconds !== undefined && object.jitterSeconds !== null)
      ? BigInt(object.jitterSeconds)
      : 0n;
    message.allowDuplicate = object.allowDuplicate ?? false;
    return message;
  },
};

function createBaseListSchedulesResponse(): ListSchedulesResponse {
  return { schedules: [] };
}

export const ListSchedulesResponse: MessageFns<ListSchedulesResponse> = {
  encode(message: ListSchedulesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.schedules) {
      ScheduleInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSchedulesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSchedulesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.schedules.push(ScheduleInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSchedulesResponse {
    return {
      schedules: globalThis.Array.isArray(object?.schedules)
        ? object.schedules.map((e: any) => ScheduleInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListSchedulesResponse): unknown {
    const obj: any = {};
    if (message.schedules?.length) {
      obj.schedules = message.schedules.map((e) => ScheduleInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListSchedulesResponse>, I>>(base?: I): ListSchedulesResponse {
    return ListSchedulesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListSchedulesResponse>, I>>(object: I): ListSchedulesResponse {
    const message = createBaseListSchedulesResponse();
    message.schedules = object.schedules?.map((e) => ScheduleInfo.fromPartial(e)) || [];
    return message;
  },
};

/**
 * Bidirectional streaming service for worker communication.
 * Workers connect and maintain a persistent stream for action dispatch/results.
 */
export type WorkerBridgeService = typeof WorkerBridgeService;
export const WorkerBridgeService = {
  attach: {
    path: "/waymark.messages.WorkerBridge/Attach" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: Envelope): Buffer => Buffer.from(Envelope.encode(value).finish()),
    requestDeserialize: (value: Buffer): Envelope => Envelope.decode(value),
    responseSerialize: (value: Envelope): Buffer => Buffer.from(Envelope.encode(value).finish()),
    responseDeserialize: (value: Buffer): Envelope => Envelope.decode(value),
  },
} as const;

export interface WorkerBridgeServer extends UntypedServiceImplementation {
  attach: handleBidiStreamingCall<Envelope, Envelope>;
}

export interface WorkerBridgeClient extends Client {
  attach(): ClientDuplexStream<Envelope, Envelope>;
  attach(options: Partial<CallOptions>): ClientDuplexStream<Envelope, Envelope>;
  attach(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<Envelope, Envelope>;
}

export const WorkerBridgeClient = makeGenericClientConstructor(
  WorkerBridgeService,
  "waymark.messages.WorkerBridge",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkerBridgeClient;
  service: typeof WorkerBridgeService;
  serviceName: string;
};

/** Workflow management service for client operations. */
export type WorkflowServiceService = typeof WorkflowServiceService;
export const WorkflowServiceService = {
  registerWorkflow: {
    path: "/waymark.messages.WorkflowService/RegisterWorkflow" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterWorkflowRequest): Buffer =>
      Buffer.from(RegisterWorkflowRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterWorkflowRequest => RegisterWorkflowRequest.decode(value),
    responseSerialize: (value: RegisterWorkflowResponse): Buffer =>
      Buffer.from(RegisterWorkflowResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterWorkflowResponse => RegisterWorkflowResponse.decode(value),
  },
  registerWorkflowBatch: {
    path: "/waymark.messages.WorkflowService/RegisterWorkflowBatch" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterWorkflowBatchRequest): Buffer =>
      Buffer.from(RegisterWorkflowBatchRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterWorkflowBatchRequest => RegisterWorkflowBatchRequest.decode(value),
    responseSerialize: (value: RegisterWorkflowBatchResponse): Buffer =>
      Buffer.from(RegisterWorkflowBatchResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterWorkflowBatchResponse => RegisterWorkflowBatchResponse.decode(value),
  },
  waitForInstance: {
    path: "/waymark.messages.WorkflowService/WaitForInstance" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: WaitForInstanceRequest): Buffer =>
      Buffer.from(WaitForInstanceRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WaitForInstanceRequest => WaitForInstanceRequest.decode(value),
    responseSerialize: (value: WaitForInstanceResponse): Buffer =>
      Buffer.from(WaitForInstanceResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): WaitForInstanceResponse => WaitForInstanceResponse.decode(value),
  },
  executeWorkflow: {
    path: "/waymark.messages.WorkflowService/ExecuteWorkflow" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: WorkflowStreamRequest): Buffer =>
      Buffer.from(WorkflowStreamRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): WorkflowStreamRequest => WorkflowStreamRequest.decode(value),
    responseSerialize: (value: WorkflowStreamResponse): Buffer =>
      Buffer.from(WorkflowStreamResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): WorkflowStreamResponse => WorkflowStreamResponse.decode(value),
  },
  /** Schedule management */
  registerSchedule: {
    path: "/waymark.messages.WorkflowService/RegisterSchedule" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterScheduleRequest): Buffer =>
      Buffer.from(RegisterScheduleRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterScheduleRequest => RegisterScheduleRequest.decode(value),
    responseSerialize: (value: RegisterScheduleResponse): Buffer =>
      Buffer.from(RegisterScheduleResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterScheduleResponse => RegisterScheduleResponse.decode(value),
  },
  updateScheduleStatus: {
    path: "/waymark.messages.WorkflowService/UpdateScheduleStatus" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UpdateScheduleStatusRequest): Buffer =>
      Buffer.from(UpdateScheduleStatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UpdateScheduleStatusRequest => UpdateScheduleStatusRequest.decode(value),
    responseSerialize: (value: UpdateScheduleStatusResponse): Buffer =>
      Buffer.from(UpdateScheduleStatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UpdateScheduleStatusResponse => UpdateScheduleStatusResponse.decode(value),
  },
  deleteSchedule: {
    path: "/waymark.messages.WorkflowService/DeleteSchedule" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: DeleteScheduleRequest): Buffer =>
      Buffer.from(DeleteScheduleRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): DeleteScheduleRequest => DeleteScheduleRequest.decode(value),
    responseSerialize: (value: DeleteScheduleResponse): Buffer =>
      Buffer.from(DeleteScheduleResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): DeleteScheduleResponse => DeleteScheduleResponse.decode(value),
  },
  listSchedules: {
    path: "/waymark.messages.WorkflowService/ListSchedules" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSchedulesRequest): Buffer => Buffer.from(ListSchedulesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSchedulesRequest => ListSchedulesRequest.decode(value),
    responseSerialize: (value: ListSchedulesResponse): Buffer =>
      Buffer.from(ListSchedulesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSchedulesResponse => ListSchedulesResponse.decode(value),
  },
} as const;

export interface WorkflowServiceServer extends UntypedServiceImplementation {
  registerWorkflow: handleUnaryCall<RegisterWorkflowRequest, RegisterWorkflowResponse>;
  registerWorkflowBatch: handleUnaryCall<RegisterWorkflowBatchRequest, RegisterWorkflowBatchResponse>;
  waitForInstance: handleUnaryCall<WaitForInstanceRequest, WaitForInstanceResponse>;
  executeWorkflow: handleBidiStreamingCall<WorkflowStreamRequest, WorkflowStreamResponse>;
  /** Schedule management */
  registerSchedule: handleUnaryCall<RegisterScheduleRequest, RegisterScheduleResponse>;
  updateScheduleStatus: handleUnaryCall<UpdateScheduleStatusRequest, UpdateScheduleStatusResponse>;
  deleteSchedule: handleUnaryCall<DeleteScheduleRequest, DeleteScheduleResponse>;
  listSchedules: handleUnaryCall<ListSchedulesRequest, ListSchedulesResponse>;
}

export interface WorkflowServiceClient extends Client {
  registerWorkflow(
    request: RegisterWorkflowRequest,
    callback: (error: ServiceError | null, response: RegisterWorkflowResponse) => void,
  ): ClientUnaryCall;
  registerWorkflow(
    request: RegisterWorkflowRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterWorkflowResponse) => void,
  ): ClientUnaryCall;
  registerWorkflow(
    request: RegisterWorkflowRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterWorkflowResponse) => void,
  ): ClientUnaryCall;
  registerWorkflowBatch(
    request: RegisterWorkflowBatchRequest,
    callback: (error: ServiceError | null, response: RegisterWorkflowBatchResponse) => void,
  ): ClientUnaryCall;
  registerWorkflowBatch(
    request: RegisterWorkflowBatchRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterWorkflowBatchResponse) => void,
  ): ClientUnaryCall;
  registerWorkflowBatch(
    request: RegisterWorkflowBatchRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterWorkflowBatchResponse) => void,
  ): ClientUnaryCall;
  waitForInstance(
    request: WaitForInstanceRequest,
    callback: (error: ServiceError | null, response: WaitForInstanceResponse) => void,
  ): ClientUnaryCall;
  waitForInstance(
    request: WaitForInstanceRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: WaitForInstanceResponse) => void,
  ): ClientUnaryCall;
  waitForInstance(
    request: WaitForInstanceRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: WaitForInstanceResponse) => void,
  ): ClientUnaryCall;
  executeWorkflow(): ClientDuplexStream<WorkflowStreamRequest, WorkflowStreamResponse>;
  executeWorkflow(options: Partial<CallOptions>): ClientDuplexStream<WorkflowStreamRequest, WorkflowStreamResponse>;
  executeWorkflow(
    metadata: Metadata,
    options?: Partial<CallOptions>,
  ): ClientDuplexStream<WorkflowStreamRequest, WorkflowStreamResponse>;
  /** Schedule management */
  registerSchedule(
    request: RegisterScheduleRequest,
    callback: (error: ServiceError | null, response: RegisterScheduleResponse) => void,
  ): ClientUnaryCall;
  registerSchedule(
    request: RegisterScheduleRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterScheduleResponse) => void,
  ): ClientUnaryCall;
  registerSchedule(
    request: RegisterScheduleRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterScheduleResponse) => void,
  ): ClientUnaryCall;
  updateScheduleStatus(
    request: UpdateScheduleStatusRequest,
    callback: (error: ServiceError | null, response: UpdateScheduleStatusResponse) => void,
  ): ClientUnaryCall;
  updateScheduleStatus(
    request: UpdateScheduleStatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UpdateScheduleStatusResponse) => void,
  ): ClientUnaryCall;
  updateScheduleStatus(
    request: UpdateScheduleStatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UpdateScheduleStatusResponse) => void,
  ): ClientUnaryCall;
  deleteSchedule(
    request: DeleteScheduleRequest,
    callback: (error: ServiceError | null, response: DeleteScheduleResponse) => void,
  ): ClientUnaryCall;
  deleteSchedule(
    request: DeleteScheduleRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: DeleteScheduleResponse) => void,
  ): ClientUnaryCall;
  deleteSchedule(
    request: DeleteScheduleRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: DeleteScheduleResponse) => void,
  ): ClientUnaryCall;
  listSchedules(
    request: ListSchedulesRequest,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
  listSchedules(
    request: ListSchedulesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
  listSchedules(
    request: ListSchedulesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSchedulesResponse) => void,
  ): ClientUnaryCall;
}

export const WorkflowServiceClient = makeGenericClientConstructor(
  WorkflowServiceService,
  "waymark.messages.WorkflowService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): WorkflowServiceClient;
  service: typeof WorkflowServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
