// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v5.29.0
// source: ast.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ActionRuntime, actionRuntimeFromJSON, actionRuntimeToJSON } from "./action.js";

export const protobufPackage = "waymark.ast";

export enum BinaryOperator {
  BINARY_OP_UNSPECIFIED = 0,
  /** BINARY_OP_ADD - Arithmetic */
  BINARY_OP_ADD = 1,
  BINARY_OP_SUB = 2,
  BINARY_OP_MUL = 3,
  BINARY_OP_DIV = 4,
  BINARY_OP_FLOOR_DIV = 5,
  BINARY_OP_MOD = 6,
  /** BINARY_OP_EQ - Comparison */
  BINARY_OP_EQ = 10,
  BINARY_OP_NE = 11,
  BINARY_OP_LT = 12,
  BINARY_OP_LE = 13,
  BINARY_OP_GT = 14,
  BINARY_OP_GE = 15,
  BINARY_OP_IN = 16,
  BINARY_OP_NOT_IN = 17,
  /** BINARY_OP_AND - Logical */
  BINARY_OP_AND = 20,
  BINARY_OP_OR = 21,
  UNRECOGNIZED = -1,
}

export function binaryOperatorFromJSON(object: any): BinaryOperator {
  switch (object) {
    case 0:
    case "BINARY_OP_UNSPECIFIED":
      return BinaryOperator.BINARY_OP_UNSPECIFIED;
    case 1:
    case "BINARY_OP_ADD":
      return BinaryOperator.BINARY_OP_ADD;
    case 2:
    case "BINARY_OP_SUB":
      return BinaryOperator.BINARY_OP_SUB;
    case 3:
    case "BINARY_OP_MUL":
      return BinaryOperator.BINARY_OP_MUL;
    case 4:
    case "BINARY_OP_DIV":
      return BinaryOperator.BINARY_OP_DIV;
    case 5:
    case "BINARY_OP_FLOOR_DIV":
      return BinaryOperator.BINARY_OP_FLOOR_DIV;
    case 6:
    case "BINARY_OP_MOD":
      return BinaryOperator.BINARY_OP_MOD;
    case 10:
    case "BINARY_OP_EQ":
      return BinaryOperator.BINARY_OP_EQ;
    case 11:
    case "BINARY_OP_NE":
      return BinaryOperator.BINARY_OP_NE;
    case 12:
    case "BINARY_OP_LT":
      return BinaryOperator.BINARY_OP_LT;
    case 13:
    case "BINARY_OP_LE":
      return BinaryOperator.BINARY_OP_LE;
    case 14:
    case "BINARY_OP_GT":
      return BinaryOperator.BINARY_OP_GT;
    case 15:
    case "BINARY_OP_GE":
      return BinaryOperator.BINARY_OP_GE;
    case 16:
    case "BINARY_OP_IN":
      return BinaryOperator.BINARY_OP_IN;
    case 17:
    case "BINARY_OP_NOT_IN":
      return BinaryOperator.BINARY_OP_NOT_IN;
    case 20:
    case "BINARY_OP_AND":
      return BinaryOperator.BINARY_OP_AND;
    case 21:
    case "BINARY_OP_OR":
      return BinaryOperator.BINARY_OP_OR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return BinaryOperator.UNRECOGNIZED;
  }
}

export function binaryOperatorToJSON(object: BinaryOperator): string {
  switch (object) {
    case BinaryOperator.BINARY_OP_UNSPECIFIED:
      return "BINARY_OP_UNSPECIFIED";
    case BinaryOperator.BINARY_OP_ADD:
      return "BINARY_OP_ADD";
    case BinaryOperator.BINARY_OP_SUB:
      return "BINARY_OP_SUB";
    case BinaryOperator.BINARY_OP_MUL:
      return "BINARY_OP_MUL";
    case BinaryOperator.BINARY_OP_DIV:
      return "BINARY_OP_DIV";
    case BinaryOperator.BINARY_OP_FLOOR_DIV:
      return "BINARY_OP_FLOOR_DIV";
    case BinaryOperator.BINARY_OP_MOD:
      return "BINARY_OP_MOD";
    case BinaryOperator.BINARY_OP_EQ:
      return "BINARY_OP_EQ";
    case BinaryOperator.BINARY_OP_NE:
      return "BINARY_OP_NE";
    case BinaryOperator.BINARY_OP_LT:
      return "BINARY_OP_LT";
    case BinaryOperator.BINARY_OP_LE:
      return "BINARY_OP_LE";
    case BinaryOperator.BINARY_OP_GT:
      return "BINARY_OP_GT";
    case BinaryOperator.BINARY_OP_GE:
      return "BINARY_OP_GE";
    case BinaryOperator.BINARY_OP_IN:
      return "BINARY_OP_IN";
    case BinaryOperator.BINARY_OP_NOT_IN:
      return "BINARY_OP_NOT_IN";
    case BinaryOperator.BINARY_OP_AND:
      return "BINARY_OP_AND";
    case BinaryOperator.BINARY_OP_OR:
      return "BINARY_OP_OR";
    case BinaryOperator.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum UnaryOperator {
  UNARY_OP_UNSPECIFIED = 0,
  UNARY_OP_NEG = 1,
  UNARY_OP_NOT = 2,
  UNRECOGNIZED = -1,
}

export function unaryOperatorFromJSON(object: any): UnaryOperator {
  switch (object) {
    case 0:
    case "UNARY_OP_UNSPECIFIED":
      return UnaryOperator.UNARY_OP_UNSPECIFIED;
    case 1:
    case "UNARY_OP_NEG":
      return UnaryOperator.UNARY_OP_NEG;
    case 2:
    case "UNARY_OP_NOT":
      return UnaryOperator.UNARY_OP_NOT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UnaryOperator.UNRECOGNIZED;
  }
}

export function unaryOperatorToJSON(object: UnaryOperator): string {
  switch (object) {
    case UnaryOperator.UNARY_OP_UNSPECIFIED:
      return "UNARY_OP_UNSPECIFIED";
    case UnaryOperator.UNARY_OP_NEG:
      return "UNARY_OP_NEG";
    case UnaryOperator.UNARY_OP_NOT:
      return "UNARY_OP_NOT";
    case UnaryOperator.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Global (built-in) functions supported by the runtime. */
export enum GlobalFunction {
  GLOBAL_FUNCTION_UNSPECIFIED = 0,
  GLOBAL_FUNCTION_RANGE = 1,
  GLOBAL_FUNCTION_LEN = 2,
  GLOBAL_FUNCTION_ENUMERATE = 3,
  GLOBAL_FUNCTION_ISEXCEPTION = 4,
  UNRECOGNIZED = -1,
}

export function globalFunctionFromJSON(object: any): GlobalFunction {
  switch (object) {
    case 0:
    case "GLOBAL_FUNCTION_UNSPECIFIED":
      return GlobalFunction.GLOBAL_FUNCTION_UNSPECIFIED;
    case 1:
    case "GLOBAL_FUNCTION_RANGE":
      return GlobalFunction.GLOBAL_FUNCTION_RANGE;
    case 2:
    case "GLOBAL_FUNCTION_LEN":
      return GlobalFunction.GLOBAL_FUNCTION_LEN;
    case 3:
    case "GLOBAL_FUNCTION_ENUMERATE":
      return GlobalFunction.GLOBAL_FUNCTION_ENUMERATE;
    case 4:
    case "GLOBAL_FUNCTION_ISEXCEPTION":
      return GlobalFunction.GLOBAL_FUNCTION_ISEXCEPTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GlobalFunction.UNRECOGNIZED;
  }
}

export function globalFunctionToJSON(object: GlobalFunction): string {
  switch (object) {
    case GlobalFunction.GLOBAL_FUNCTION_UNSPECIFIED:
      return "GLOBAL_FUNCTION_UNSPECIFIED";
    case GlobalFunction.GLOBAL_FUNCTION_RANGE:
      return "GLOBAL_FUNCTION_RANGE";
    case GlobalFunction.GLOBAL_FUNCTION_LEN:
      return "GLOBAL_FUNCTION_LEN";
    case GlobalFunction.GLOBAL_FUNCTION_ENUMERATE:
      return "GLOBAL_FUNCTION_ENUMERATE";
    case GlobalFunction.GLOBAL_FUNCTION_ISEXCEPTION:
      return "GLOBAL_FUNCTION_ISEXCEPTION";
    case GlobalFunction.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** A complete Waymark program (one or more function definitions) */
export interface Program {
  functions: FunctionDef[];
}

/** Function definition with explicit input/output declarations */
export interface FunctionDef {
  name: string;
  io: IoDecl | undefined;
  body: Block | undefined;
  span: Span | undefined;
}

/** Input/output declaration: fn foo(input: [a, b], output: [c]) */
export interface IoDecl {
  inputs: string[];
  outputs: string[];
  span: Span | undefined;
}

/** A block of statements (function body - no call restrictions) */
export interface Block {
  statements: Statement[];
  span: Span | undefined;
}

export interface Statement {
  kind:
    | { $case: "assignment"; value: Assignment }
    | { $case: "actionCall"; value: ActionCall }
    | { $case: "spreadAction"; value: SpreadAction }
    | { $case: "parallelBlock"; value: ParallelBlock }
    | { $case: "forLoop"; value: ForLoop }
    | { $case: "conditional"; value: Conditional }
    | { $case: "tryExcept"; value: TryExcept }
    | { $case: "returnStmt"; value: ReturnStmt }
    | { $case: "exprStmt"; value: ExprStmt }
    | { $case: "breakStmt"; value: BreakStmt }
    | { $case: "continueStmt"; value: ContinueStmt }
    | { $case: "whileLoop"; value: WhileLoop }
    | { $case: "sleepStmt"; value: SleepStmt }
    | undefined;
  span: Span | undefined;
}

/** Break statement (exits innermost loop) */
export interface BreakStmt {
}

/** Continue statement (skips to next iteration of innermost loop) */
export interface ContinueStmt {
}

/** Variable assignment: x = expr */
export interface Assignment {
  /** Single variable or tuple unpacking (a, b = expr) */
  targets: string[];
  value: Expr | undefined;
}

/**
 * Action call: @action(kwargs) [retry_policy] [timeout]
 * Can be used as:
 * - Statement (side effect only): @notify(user=x)
 * - Expression in Assignment (with binding): result = @fetch(id=x)
 * - Expression in Assignment (with unpacking): a, b = @get_pair()
 */
export interface ActionCall {
  actionName: string;
  kwargs: Kwarg[];
  policies: PolicyBracket[];
  /** Module containing the action */
  moduleName?: string | undefined;
  runtime: ActionRuntime;
}

/**
 * Spread action statement (side effect only, no result capture)
 * For result capture, use Assignment with SpreadExpr
 */
export interface SpreadAction {
  /** The collection to iterate */
  collection:
    | Expr
    | undefined;
  /** Variable name for each item */
  loopVar: string;
  /** The action to call per item */
  action: ActionCall | undefined;
}

/**
 * Parallel block statement (side effect only, no result capture)
 * For result capture, use Assignment with ParallelExpr
 */
export interface ParallelBlock {
  /** Actions or function calls to run in parallel */
  calls: Call[];
}

/** A call that can be either action or function (for parallel blocks) */
export interface Call {
  kind: { $case: "action"; value: ActionCall } | { $case: "function"; value: FunctionCall } | undefined;
}

/** For loop: for item in items: ... */
export interface ForLoop {
  /** Loop variable(s) for unpacking */
  loopVars: string[];
  iterable: Expr | undefined;
  blockBody: Block | undefined;
}

/** While loop: while condition: ... */
export interface WhileLoop {
  condition: Expr | undefined;
  blockBody: Block | undefined;
}

/** Conditional: if/elif/else */
export interface Conditional {
  ifBranch: IfBranch | undefined;
  elifBranches: ElifBranch[];
  elseBranch?: ElseBranch | undefined;
}

export interface IfBranch {
  condition: Expr | undefined;
  span: Span | undefined;
  blockBody: Block | undefined;
}

export interface ElifBranch {
  condition: Expr | undefined;
  span: Span | undefined;
  blockBody: Block | undefined;
}

export interface ElseBranch {
  span: Span | undefined;
  blockBody: Block | undefined;
}

/** Try/except block */
export interface TryExcept {
  handlers: ExceptHandler[];
  tryBlock: Block | undefined;
}

export interface ExceptHandler {
  /** Empty = catch all */
  exceptionTypes: string[];
  span: Span | undefined;
  blockBody:
    | Block
    | undefined;
  /** Variable name for "except ... as var" */
  exceptionVar?: string | undefined;
}

/** Return statement */
export interface ReturnStmt {
  value?: Expr | undefined;
}

/** Expression as statement */
export interface ExprStmt {
  expr: Expr | undefined;
}

/** Sleep statement (durable sleep handled by scheduler) */
export interface SleepStmt {
  duration?: Expr | undefined;
}

export interface Expr {
  kind:
    | { $case: "literal"; value: Literal }
    | { $case: "variable"; value: Variable }
    | { $case: "binaryOp"; value: BinaryOp }
    | { $case: "unaryOp"; value: UnaryOp }
    | { $case: "list"; value: ListExpr }
    | { $case: "dict"; value: DictExpr }
    | { $case: "index"; value: IndexAccess }
    | { $case: "dot"; value: DotAccess }
    | { $case: "functionCall"; value: FunctionCall }
    | { $case: "actionCall"; value: ActionCall }
    | //
    /** Parallel block as expression */
    { $case: "parallelExpr"; value: ParallelExpr }
    | //
    /** Spread action as expression */
    { $case: "spreadExpr"; value: SpreadExpr }
    | undefined;
  span: Span | undefined;
}

/** Literals: 42, 3.14, "hello", True, False */
export interface Literal {
  value:
    | { $case: "intValue"; value: bigint }
    | { $case: "floatValue"; value: number }
    | { $case: "stringValue"; value: string }
    | { $case: "boolValue"; value: boolean }
    | //
    /** true if None */
    { $case: "isNone"; value: boolean }
    | undefined;
}

/** Variable reference: foo */
export interface Variable {
  name: string;
}

/** Binary operation: a + b, x and y, etc. */
export interface BinaryOp {
  left: Expr | undefined;
  op: BinaryOperator;
  right: Expr | undefined;
}

/** Unary operation: -x, not x */
export interface UnaryOp {
  op: UnaryOperator;
  operand: Expr | undefined;
}

/** List literal: [1, 2, 3] */
export interface ListExpr {
  elements: Expr[];
}

/** Dict literal: {"a": 1, "b": 2} */
export interface DictExpr {
  entries: DictEntry[];
}

export interface DictEntry {
  key: Expr | undefined;
  value: Expr | undefined;
}

/** Index access: items[0] */
export interface IndexAccess {
  object: Expr | undefined;
  index: Expr | undefined;
}

/** Dot access: obj.attr */
export interface DotAccess {
  object: Expr | undefined;
  attribute: string;
}

/**
 * Function call: range(10), len(items)
 * Built-in functions can use positional args, user functions use kwargs
 */
export interface FunctionCall {
  name: string;
  /** Positional arguments (for built-ins) */
  args: Expr[];
  /** Keyword arguments */
  kwargs: Kwarg[];
  /** Global function identifier (if applicable) */
  globalFunction: GlobalFunction;
}

/** Keyword argument: name=value */
export interface Kwarg {
  name: string;
  value: Expr | undefined;
}

/**
 * Parallel expression: concurrent execution of multiple calls, returns list
 * Used in assignments: a, b = parallel: @x() @y()
 */
export interface ParallelExpr {
  /** Actions or function calls to run in parallel */
  calls: Call[];
}

/**
 * Spread expression: parallel execution over a collection, returns list
 * Used in assignments: results = spread items:item -> @action(item=item)
 */
export interface SpreadExpr {
  /** The collection to iterate */
  collection:
    | Expr
    | undefined;
  /** Variable name for each item */
  loopVar: string;
  /** The action to call per item */
  action: ActionCall | undefined;
}

/** Policy bracket on an action call */
export interface PolicyBracket {
  kind: { $case: "retry"; value: RetryPolicy } | { $case: "timeout"; value: TimeoutPolicy } | undefined;
}

/** Retry policy: [ExceptionType -> retry: 3, backoff: 60s] */
export interface RetryPolicy {
  /** Empty = catch all */
  exceptionTypes: string[];
  maxRetries: number;
  backoff: Duration | undefined;
}

/** Timeout policy: [timeout: 2m] */
export interface TimeoutPolicy {
  timeout: Duration | undefined;
}

/** Duration value (e.g., 30s, 2m, 1h) */
export interface Duration {
  seconds: bigint;
}

/** Source span for error reporting */
export interface Span {
  startLine: number;
  startCol: number;
  endLine: number;
  endCol: number;
}

function createBaseProgram(): Program {
  return { functions: [] };
}

export const Program: MessageFns<Program> = {
  encode(message: Program, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.functions) {
      FunctionDef.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Program {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProgram();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.functions.push(FunctionDef.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Program {
    return {
      functions: globalThis.Array.isArray(object?.functions)
        ? object.functions.map((e: any) => FunctionDef.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Program): unknown {
    const obj: any = {};
    if (message.functions?.length) {
      obj.functions = message.functions.map((e) => FunctionDef.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Program>, I>>(base?: I): Program {
    return Program.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Program>, I>>(object: I): Program {
    const message = createBaseProgram();
    message.functions = object.functions?.map((e) => FunctionDef.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFunctionDef(): FunctionDef {
  return { name: "", io: undefined, body: undefined, span: undefined };
}

export const FunctionDef: MessageFns<FunctionDef> = {
  encode(message: FunctionDef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.io !== undefined) {
      IoDecl.encode(message.io, writer.uint32(18).fork()).join();
    }
    if (message.body !== undefined) {
      Block.encode(message.body, writer.uint32(26).fork()).join();
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FunctionDef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFunctionDef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.io = IoDecl.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.body = Block.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FunctionDef {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      io: isSet(object.io) ? IoDecl.fromJSON(object.io) : undefined,
      body: isSet(object.body) ? Block.fromJSON(object.body) : undefined,
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
    };
  },

  toJSON(message: FunctionDef): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.io !== undefined) {
      obj.io = IoDecl.toJSON(message.io);
    }
    if (message.body !== undefined) {
      obj.body = Block.toJSON(message.body);
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FunctionDef>, I>>(base?: I): FunctionDef {
    return FunctionDef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FunctionDef>, I>>(object: I): FunctionDef {
    const message = createBaseFunctionDef();
    message.name = object.name ?? "";
    message.io = (object.io !== undefined && object.io !== null) ? IoDecl.fromPartial(object.io) : undefined;
    message.body = (object.body !== undefined && object.body !== null) ? Block.fromPartial(object.body) : undefined;
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    return message;
  },
};

function createBaseIoDecl(): IoDecl {
  return { inputs: [], outputs: [], span: undefined };
}

export const IoDecl: MessageFns<IoDecl> = {
  encode(message: IoDecl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.inputs) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.outputs) {
      writer.uint32(18).string(v!);
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IoDecl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIoDecl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inputs.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputs.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IoDecl {
    return {
      inputs: globalThis.Array.isArray(object?.inputs) ? object.inputs.map((e: any) => globalThis.String(e)) : [],
      outputs: globalThis.Array.isArray(object?.outputs) ? object.outputs.map((e: any) => globalThis.String(e)) : [],
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
    };
  },

  toJSON(message: IoDecl): unknown {
    const obj: any = {};
    if (message.inputs?.length) {
      obj.inputs = message.inputs;
    }
    if (message.outputs?.length) {
      obj.outputs = message.outputs;
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IoDecl>, I>>(base?: I): IoDecl {
    return IoDecl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IoDecl>, I>>(object: I): IoDecl {
    const message = createBaseIoDecl();
    message.inputs = object.inputs?.map((e) => e) || [];
    message.outputs = object.outputs?.map((e) => e) || [];
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    return message;
  },
};

function createBaseBlock(): Block {
  return { statements: [], span: undefined };
}

export const Block: MessageFns<Block> = {
  encode(message: Block, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.statements) {
      Statement.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Block {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.statements.push(Statement.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Block {
    return {
      statements: globalThis.Array.isArray(object?.statements)
        ? object.statements.map((e: any) => Statement.fromJSON(e))
        : [],
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
    };
  },

  toJSON(message: Block): unknown {
    const obj: any = {};
    if (message.statements?.length) {
      obj.statements = message.statements.map((e) => Statement.toJSON(e));
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Block>, I>>(base?: I): Block {
    return Block.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Block>, I>>(object: I): Block {
    const message = createBaseBlock();
    message.statements = object.statements?.map((e) => Statement.fromPartial(e)) || [];
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    return message;
  },
};

function createBaseStatement(): Statement {
  return { kind: undefined, span: undefined };
}

export const Statement: MessageFns<Statement> = {
  encode(message: Statement, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "assignment":
        Assignment.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "actionCall":
        ActionCall.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
      case "spreadAction":
        SpreadAction.encode(message.kind.value, writer.uint32(26).fork()).join();
        break;
      case "parallelBlock":
        ParallelBlock.encode(message.kind.value, writer.uint32(34).fork()).join();
        break;
      case "forLoop":
        ForLoop.encode(message.kind.value, writer.uint32(42).fork()).join();
        break;
      case "conditional":
        Conditional.encode(message.kind.value, writer.uint32(50).fork()).join();
        break;
      case "tryExcept":
        TryExcept.encode(message.kind.value, writer.uint32(58).fork()).join();
        break;
      case "returnStmt":
        ReturnStmt.encode(message.kind.value, writer.uint32(66).fork()).join();
        break;
      case "exprStmt":
        ExprStmt.encode(message.kind.value, writer.uint32(74).fork()).join();
        break;
      case "breakStmt":
        BreakStmt.encode(message.kind.value, writer.uint32(90).fork()).join();
        break;
      case "continueStmt":
        ContinueStmt.encode(message.kind.value, writer.uint32(98).fork()).join();
        break;
      case "whileLoop":
        WhileLoop.encode(message.kind.value, writer.uint32(106).fork()).join();
        break;
      case "sleepStmt":
        SleepStmt.encode(message.kind.value, writer.uint32(114).fork()).join();
        break;
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Statement {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatement();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "assignment", value: Assignment.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "actionCall", value: ActionCall.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = { $case: "spreadAction", value: SpreadAction.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kind = { $case: "parallelBlock", value: ParallelBlock.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.kind = { $case: "forLoop", value: ForLoop.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = { $case: "conditional", value: Conditional.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.kind = { $case: "tryExcept", value: TryExcept.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.kind = { $case: "returnStmt", value: ReturnStmt.decode(reader, reader.uint32()) };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.kind = { $case: "exprStmt", value: ExprStmt.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.kind = { $case: "breakStmt", value: BreakStmt.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.kind = { $case: "continueStmt", value: ContinueStmt.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.kind = { $case: "whileLoop", value: WhileLoop.decode(reader, reader.uint32()) };
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.kind = { $case: "sleepStmt", value: SleepStmt.decode(reader, reader.uint32()) };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Statement {
    return {
      kind: isSet(object.assignment)
        ? { $case: "assignment", value: Assignment.fromJSON(object.assignment) }
        : isSet(object.actionCall)
        ? { $case: "actionCall", value: ActionCall.fromJSON(object.actionCall) }
        : isSet(object.action_call)
        ? { $case: "actionCall", value: ActionCall.fromJSON(object.action_call) }
        : isSet(object.spreadAction)
        ? { $case: "spreadAction", value: SpreadAction.fromJSON(object.spreadAction) }
        : isSet(object.spread_action)
        ? { $case: "spreadAction", value: SpreadAction.fromJSON(object.spread_action) }
        : isSet(object.parallelBlock)
        ? { $case: "parallelBlock", value: ParallelBlock.fromJSON(object.parallelBlock) }
        : isSet(object.parallel_block)
        ? { $case: "parallelBlock", value: ParallelBlock.fromJSON(object.parallel_block) }
        : isSet(object.forLoop)
        ? { $case: "forLoop", value: ForLoop.fromJSON(object.forLoop) }
        : isSet(object.for_loop)
        ? { $case: "forLoop", value: ForLoop.fromJSON(object.for_loop) }
        : isSet(object.conditional)
        ? { $case: "conditional", value: Conditional.fromJSON(object.conditional) }
        : isSet(object.tryExcept)
        ? { $case: "tryExcept", value: TryExcept.fromJSON(object.tryExcept) }
        : isSet(object.try_except)
        ? { $case: "tryExcept", value: TryExcept.fromJSON(object.try_except) }
        : isSet(object.returnStmt)
        ? { $case: "returnStmt", value: ReturnStmt.fromJSON(object.returnStmt) }
        : isSet(object.return_stmt)
        ? { $case: "returnStmt", value: ReturnStmt.fromJSON(object.return_stmt) }
        : isSet(object.exprStmt)
        ? { $case: "exprStmt", value: ExprStmt.fromJSON(object.exprStmt) }
        : isSet(object.expr_stmt)
        ? { $case: "exprStmt", value: ExprStmt.fromJSON(object.expr_stmt) }
        : isSet(object.breakStmt)
        ? { $case: "breakStmt", value: BreakStmt.fromJSON(object.breakStmt) }
        : isSet(object.break_stmt)
        ? { $case: "breakStmt", value: BreakStmt.fromJSON(object.break_stmt) }
        : isSet(object.continueStmt)
        ? { $case: "continueStmt", value: ContinueStmt.fromJSON(object.continueStmt) }
        : isSet(object.continue_stmt)
        ? { $case: "continueStmt", value: ContinueStmt.fromJSON(object.continue_stmt) }
        : isSet(object.whileLoop)
        ? { $case: "whileLoop", value: WhileLoop.fromJSON(object.whileLoop) }
        : isSet(object.while_loop)
        ? { $case: "whileLoop", value: WhileLoop.fromJSON(object.while_loop) }
        : isSet(object.sleepStmt)
        ? { $case: "sleepStmt", value: SleepStmt.fromJSON(object.sleepStmt) }
        : isSet(object.sleep_stmt)
        ? { $case: "sleepStmt", value: SleepStmt.fromJSON(object.sleep_stmt) }
        : undefined,
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
    };
  },

  toJSON(message: Statement): unknown {
    const obj: any = {};
    if (message.kind?.$case === "assignment") {
      obj.assignment = Assignment.toJSON(message.kind.value);
    } else if (message.kind?.$case === "actionCall") {
      obj.actionCall = ActionCall.toJSON(message.kind.value);
    } else if (message.kind?.$case === "spreadAction") {
      obj.spreadAction = SpreadAction.toJSON(message.kind.value);
    } else if (message.kind?.$case === "parallelBlock") {
      obj.parallelBlock = ParallelBlock.toJSON(message.kind.value);
    } else if (message.kind?.$case === "forLoop") {
      obj.forLoop = ForLoop.toJSON(message.kind.value);
    } else if (message.kind?.$case === "conditional") {
      obj.conditional = Conditional.toJSON(message.kind.value);
    } else if (message.kind?.$case === "tryExcept") {
      obj.tryExcept = TryExcept.toJSON(message.kind.value);
    } else if (message.kind?.$case === "returnStmt") {
      obj.returnStmt = ReturnStmt.toJSON(message.kind.value);
    } else if (message.kind?.$case === "exprStmt") {
      obj.exprStmt = ExprStmt.toJSON(message.kind.value);
    } else if (message.kind?.$case === "breakStmt") {
      obj.breakStmt = BreakStmt.toJSON(message.kind.value);
    } else if (message.kind?.$case === "continueStmt") {
      obj.continueStmt = ContinueStmt.toJSON(message.kind.value);
    } else if (message.kind?.$case === "whileLoop") {
      obj.whileLoop = WhileLoop.toJSON(message.kind.value);
    } else if (message.kind?.$case === "sleepStmt") {
      obj.sleepStmt = SleepStmt.toJSON(message.kind.value);
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Statement>, I>>(base?: I): Statement {
    return Statement.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Statement>, I>>(object: I): Statement {
    const message = createBaseStatement();
    switch (object.kind?.$case) {
      case "assignment": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "assignment", value: Assignment.fromPartial(object.kind.value) };
        }
        break;
      }
      case "actionCall": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "actionCall", value: ActionCall.fromPartial(object.kind.value) };
        }
        break;
      }
      case "spreadAction": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "spreadAction", value: SpreadAction.fromPartial(object.kind.value) };
        }
        break;
      }
      case "parallelBlock": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "parallelBlock", value: ParallelBlock.fromPartial(object.kind.value) };
        }
        break;
      }
      case "forLoop": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "forLoop", value: ForLoop.fromPartial(object.kind.value) };
        }
        break;
      }
      case "conditional": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "conditional", value: Conditional.fromPartial(object.kind.value) };
        }
        break;
      }
      case "tryExcept": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "tryExcept", value: TryExcept.fromPartial(object.kind.value) };
        }
        break;
      }
      case "returnStmt": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "returnStmt", value: ReturnStmt.fromPartial(object.kind.value) };
        }
        break;
      }
      case "exprStmt": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "exprStmt", value: ExprStmt.fromPartial(object.kind.value) };
        }
        break;
      }
      case "breakStmt": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "breakStmt", value: BreakStmt.fromPartial(object.kind.value) };
        }
        break;
      }
      case "continueStmt": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "continueStmt", value: ContinueStmt.fromPartial(object.kind.value) };
        }
        break;
      }
      case "whileLoop": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "whileLoop", value: WhileLoop.fromPartial(object.kind.value) };
        }
        break;
      }
      case "sleepStmt": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "sleepStmt", value: SleepStmt.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    return message;
  },
};

function createBaseBreakStmt(): BreakStmt {
  return {};
}

export const BreakStmt: MessageFns<BreakStmt> = {
  encode(_: BreakStmt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BreakStmt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBreakStmt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): BreakStmt {
    return {};
  },

  toJSON(_: BreakStmt): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<BreakStmt>, I>>(base?: I): BreakStmt {
    return BreakStmt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BreakStmt>, I>>(_: I): BreakStmt {
    const message = createBaseBreakStmt();
    return message;
  },
};

function createBaseContinueStmt(): ContinueStmt {
  return {};
}

export const ContinueStmt: MessageFns<ContinueStmt> = {
  encode(_: ContinueStmt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContinueStmt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContinueStmt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ContinueStmt {
    return {};
  },

  toJSON(_: ContinueStmt): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ContinueStmt>, I>>(base?: I): ContinueStmt {
    return ContinueStmt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContinueStmt>, I>>(_: I): ContinueStmt {
    const message = createBaseContinueStmt();
    return message;
  },
};

function createBaseAssignment(): Assignment {
  return { targets: [], value: undefined };
}

export const Assignment: MessageFns<Assignment> = {
  encode(message: Assignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.targets) {
      writer.uint32(10).string(v!);
    }
    if (message.value !== undefined) {
      Expr.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Assignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.targets.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Assignment {
    return {
      targets: globalThis.Array.isArray(object?.targets) ? object.targets.map((e: any) => globalThis.String(e)) : [],
      value: isSet(object.value) ? Expr.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Assignment): unknown {
    const obj: any = {};
    if (message.targets?.length) {
      obj.targets = message.targets;
    }
    if (message.value !== undefined) {
      obj.value = Expr.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Assignment>, I>>(base?: I): Assignment {
    return Assignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Assignment>, I>>(object: I): Assignment {
    const message = createBaseAssignment();
    message.targets = object.targets?.map((e) => e) || [];
    message.value = (object.value !== undefined && object.value !== null) ? Expr.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseActionCall(): ActionCall {
  return { actionName: "", kwargs: [], policies: [], moduleName: undefined, runtime: 0 };
}

export const ActionCall: MessageFns<ActionCall> = {
  encode(message: ActionCall, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.actionName !== "") {
      writer.uint32(10).string(message.actionName);
    }
    for (const v of message.kwargs) {
      Kwarg.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.policies) {
      PolicyBracket.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.moduleName !== undefined) {
      writer.uint32(34).string(message.moduleName);
    }
    if (message.runtime !== 0) {
      writer.uint32(40).int32(message.runtime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ActionCall {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionCall();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.actionName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kwargs.push(Kwarg.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.policies.push(PolicyBracket.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.moduleName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.runtime = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionCall {
    return {
      actionName: isSet(object.actionName)
        ? globalThis.String(object.actionName)
        : isSet(object.action_name)
        ? globalThis.String(object.action_name)
        : "",
      kwargs: globalThis.Array.isArray(object?.kwargs) ? object.kwargs.map((e: any) => Kwarg.fromJSON(e)) : [],
      policies: globalThis.Array.isArray(object?.policies)
        ? object.policies.map((e: any) => PolicyBracket.fromJSON(e))
        : [],
      moduleName: isSet(object.moduleName)
        ? globalThis.String(object.moduleName)
        : isSet(object.module_name)
        ? globalThis.String(object.module_name)
        : undefined,
      runtime: isSet(object.runtime) ? actionRuntimeFromJSON(object.runtime) : 0,
    };
  },

  toJSON(message: ActionCall): unknown {
    const obj: any = {};
    if (message.actionName !== "") {
      obj.actionName = message.actionName;
    }
    if (message.kwargs?.length) {
      obj.kwargs = message.kwargs.map((e) => Kwarg.toJSON(e));
    }
    if (message.policies?.length) {
      obj.policies = message.policies.map((e) => PolicyBracket.toJSON(e));
    }
    if (message.moduleName !== undefined) {
      obj.moduleName = message.moduleName;
    }
    if (message.runtime !== 0) {
      obj.runtime = actionRuntimeToJSON(message.runtime);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionCall>, I>>(base?: I): ActionCall {
    return ActionCall.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionCall>, I>>(object: I): ActionCall {
    const message = createBaseActionCall();
    message.actionName = object.actionName ?? "";
    message.kwargs = object.kwargs?.map((e) => Kwarg.fromPartial(e)) || [];
    message.policies = object.policies?.map((e) => PolicyBracket.fromPartial(e)) || [];
    message.moduleName = object.moduleName ?? undefined;
    message.runtime = object.runtime ?? 0;
    return message;
  },
};

function createBaseSpreadAction(): SpreadAction {
  return { collection: undefined, loopVar: "", action: undefined };
}

export const SpreadAction: MessageFns<SpreadAction> = {
  encode(message: SpreadAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collection !== undefined) {
      Expr.encode(message.collection, writer.uint32(10).fork()).join();
    }
    if (message.loopVar !== "") {
      writer.uint32(18).string(message.loopVar);
    }
    if (message.action !== undefined) {
      ActionCall.encode(message.action, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpreadAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpreadAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collection = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.loopVar = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = ActionCall.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpreadAction {
    return {
      collection: isSet(object.collection) ? Expr.fromJSON(object.collection) : undefined,
      loopVar: isSet(object.loopVar)
        ? globalThis.String(object.loopVar)
        : isSet(object.loop_var)
        ? globalThis.String(object.loop_var)
        : "",
      action: isSet(object.action) ? ActionCall.fromJSON(object.action) : undefined,
    };
  },

  toJSON(message: SpreadAction): unknown {
    const obj: any = {};
    if (message.collection !== undefined) {
      obj.collection = Expr.toJSON(message.collection);
    }
    if (message.loopVar !== "") {
      obj.loopVar = message.loopVar;
    }
    if (message.action !== undefined) {
      obj.action = ActionCall.toJSON(message.action);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpreadAction>, I>>(base?: I): SpreadAction {
    return SpreadAction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpreadAction>, I>>(object: I): SpreadAction {
    const message = createBaseSpreadAction();
    message.collection = (object.collection !== undefined && object.collection !== null)
      ? Expr.fromPartial(object.collection)
      : undefined;
    message.loopVar = object.loopVar ?? "";
    message.action = (object.action !== undefined && object.action !== null)
      ? ActionCall.fromPartial(object.action)
      : undefined;
    return message;
  },
};

function createBaseParallelBlock(): ParallelBlock {
  return { calls: [] };
}

export const ParallelBlock: MessageFns<ParallelBlock> = {
  encode(message: ParallelBlock, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.calls) {
      Call.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParallelBlock {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParallelBlock();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.calls.push(Call.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParallelBlock {
    return { calls: globalThis.Array.isArray(object?.calls) ? object.calls.map((e: any) => Call.fromJSON(e)) : [] };
  },

  toJSON(message: ParallelBlock): unknown {
    const obj: any = {};
    if (message.calls?.length) {
      obj.calls = message.calls.map((e) => Call.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParallelBlock>, I>>(base?: I): ParallelBlock {
    return ParallelBlock.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParallelBlock>, I>>(object: I): ParallelBlock {
    const message = createBaseParallelBlock();
    message.calls = object.calls?.map((e) => Call.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCall(): Call {
  return { kind: undefined };
}

export const Call: MessageFns<Call> = {
  encode(message: Call, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "action":
        ActionCall.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "function":
        FunctionCall.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Call {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCall();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "action", value: ActionCall.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "function", value: FunctionCall.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Call {
    return {
      kind: isSet(object.action)
        ? { $case: "action", value: ActionCall.fromJSON(object.action) }
        : isSet(object.function)
        ? { $case: "function", value: FunctionCall.fromJSON(object.function) }
        : undefined,
    };
  },

  toJSON(message: Call): unknown {
    const obj: any = {};
    if (message.kind?.$case === "action") {
      obj.action = ActionCall.toJSON(message.kind.value);
    } else if (message.kind?.$case === "function") {
      obj.function = FunctionCall.toJSON(message.kind.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Call>, I>>(base?: I): Call {
    return Call.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Call>, I>>(object: I): Call {
    const message = createBaseCall();
    switch (object.kind?.$case) {
      case "action": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "action", value: ActionCall.fromPartial(object.kind.value) };
        }
        break;
      }
      case "function": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "function", value: FunctionCall.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseForLoop(): ForLoop {
  return { loopVars: [], iterable: undefined, blockBody: undefined };
}

export const ForLoop: MessageFns<ForLoop> = {
  encode(message: ForLoop, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.loopVars) {
      writer.uint32(10).string(v!);
    }
    if (message.iterable !== undefined) {
      Expr.encode(message.iterable, writer.uint32(18).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ForLoop {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseForLoop();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.loopVars.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.iterable = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ForLoop {
    return {
      loopVars: globalThis.Array.isArray(object?.loopVars)
        ? object.loopVars.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.loop_vars)
        ? object.loop_vars.map((e: any) => globalThis.String(e))
        : [],
      iterable: isSet(object.iterable) ? Expr.fromJSON(object.iterable) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
    };
  },

  toJSON(message: ForLoop): unknown {
    const obj: any = {};
    if (message.loopVars?.length) {
      obj.loopVars = message.loopVars;
    }
    if (message.iterable !== undefined) {
      obj.iterable = Expr.toJSON(message.iterable);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ForLoop>, I>>(base?: I): ForLoop {
    return ForLoop.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ForLoop>, I>>(object: I): ForLoop {
    const message = createBaseForLoop();
    message.loopVars = object.loopVars?.map((e) => e) || [];
    message.iterable = (object.iterable !== undefined && object.iterable !== null)
      ? Expr.fromPartial(object.iterable)
      : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    return message;
  },
};

function createBaseWhileLoop(): WhileLoop {
  return { condition: undefined, blockBody: undefined };
}

export const WhileLoop: MessageFns<WhileLoop> = {
  encode(message: WhileLoop, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.condition !== undefined) {
      Expr.encode(message.condition, writer.uint32(10).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WhileLoop {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWhileLoop();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.condition = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WhileLoop {
    return {
      condition: isSet(object.condition) ? Expr.fromJSON(object.condition) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
    };
  },

  toJSON(message: WhileLoop): unknown {
    const obj: any = {};
    if (message.condition !== undefined) {
      obj.condition = Expr.toJSON(message.condition);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WhileLoop>, I>>(base?: I): WhileLoop {
    return WhileLoop.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WhileLoop>, I>>(object: I): WhileLoop {
    const message = createBaseWhileLoop();
    message.condition = (object.condition !== undefined && object.condition !== null)
      ? Expr.fromPartial(object.condition)
      : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    return message;
  },
};

function createBaseConditional(): Conditional {
  return { ifBranch: undefined, elifBranches: [], elseBranch: undefined };
}

export const Conditional: MessageFns<Conditional> = {
  encode(message: Conditional, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ifBranch !== undefined) {
      IfBranch.encode(message.ifBranch, writer.uint32(10).fork()).join();
    }
    for (const v of message.elifBranches) {
      ElifBranch.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.elseBranch !== undefined) {
      ElseBranch.encode(message.elseBranch, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Conditional {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConditional();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ifBranch = IfBranch.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.elifBranches.push(ElifBranch.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.elseBranch = ElseBranch.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Conditional {
    return {
      ifBranch: isSet(object.ifBranch)
        ? IfBranch.fromJSON(object.ifBranch)
        : isSet(object.if_branch)
        ? IfBranch.fromJSON(object.if_branch)
        : undefined,
      elifBranches: globalThis.Array.isArray(object?.elifBranches)
        ? object.elifBranches.map((e: any) => ElifBranch.fromJSON(e))
        : globalThis.Array.isArray(object?.elif_branches)
        ? object.elif_branches.map((e: any) => ElifBranch.fromJSON(e))
        : [],
      elseBranch: isSet(object.elseBranch)
        ? ElseBranch.fromJSON(object.elseBranch)
        : isSet(object.else_branch)
        ? ElseBranch.fromJSON(object.else_branch)
        : undefined,
    };
  },

  toJSON(message: Conditional): unknown {
    const obj: any = {};
    if (message.ifBranch !== undefined) {
      obj.ifBranch = IfBranch.toJSON(message.ifBranch);
    }
    if (message.elifBranches?.length) {
      obj.elifBranches = message.elifBranches.map((e) => ElifBranch.toJSON(e));
    }
    if (message.elseBranch !== undefined) {
      obj.elseBranch = ElseBranch.toJSON(message.elseBranch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Conditional>, I>>(base?: I): Conditional {
    return Conditional.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Conditional>, I>>(object: I): Conditional {
    const message = createBaseConditional();
    message.ifBranch = (object.ifBranch !== undefined && object.ifBranch !== null)
      ? IfBranch.fromPartial(object.ifBranch)
      : undefined;
    message.elifBranches = object.elifBranches?.map((e) => ElifBranch.fromPartial(e)) || [];
    message.elseBranch = (object.elseBranch !== undefined && object.elseBranch !== null)
      ? ElseBranch.fromPartial(object.elseBranch)
      : undefined;
    return message;
  },
};

function createBaseIfBranch(): IfBranch {
  return { condition: undefined, span: undefined, blockBody: undefined };
}

export const IfBranch: MessageFns<IfBranch> = {
  encode(message: IfBranch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.condition !== undefined) {
      Expr.encode(message.condition, writer.uint32(10).fork()).join();
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(18).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IfBranch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIfBranch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.condition = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IfBranch {
    return {
      condition: isSet(object.condition) ? Expr.fromJSON(object.condition) : undefined,
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
    };
  },

  toJSON(message: IfBranch): unknown {
    const obj: any = {};
    if (message.condition !== undefined) {
      obj.condition = Expr.toJSON(message.condition);
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IfBranch>, I>>(base?: I): IfBranch {
    return IfBranch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IfBranch>, I>>(object: I): IfBranch {
    const message = createBaseIfBranch();
    message.condition = (object.condition !== undefined && object.condition !== null)
      ? Expr.fromPartial(object.condition)
      : undefined;
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    return message;
  },
};

function createBaseElifBranch(): ElifBranch {
  return { condition: undefined, span: undefined, blockBody: undefined };
}

export const ElifBranch: MessageFns<ElifBranch> = {
  encode(message: ElifBranch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.condition !== undefined) {
      Expr.encode(message.condition, writer.uint32(10).fork()).join();
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(18).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ElifBranch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseElifBranch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.condition = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ElifBranch {
    return {
      condition: isSet(object.condition) ? Expr.fromJSON(object.condition) : undefined,
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
    };
  },

  toJSON(message: ElifBranch): unknown {
    const obj: any = {};
    if (message.condition !== undefined) {
      obj.condition = Expr.toJSON(message.condition);
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ElifBranch>, I>>(base?: I): ElifBranch {
    return ElifBranch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ElifBranch>, I>>(object: I): ElifBranch {
    const message = createBaseElifBranch();
    message.condition = (object.condition !== undefined && object.condition !== null)
      ? Expr.fromPartial(object.condition)
      : undefined;
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    return message;
  },
};

function createBaseElseBranch(): ElseBranch {
  return { span: undefined, blockBody: undefined };
}

export const ElseBranch: MessageFns<ElseBranch> = {
  encode(message: ElseBranch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(10).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ElseBranch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseElseBranch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ElseBranch {
    return {
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
    };
  },

  toJSON(message: ElseBranch): unknown {
    const obj: any = {};
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ElseBranch>, I>>(base?: I): ElseBranch {
    return ElseBranch.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ElseBranch>, I>>(object: I): ElseBranch {
    const message = createBaseElseBranch();
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    return message;
  },
};

function createBaseTryExcept(): TryExcept {
  return { handlers: [], tryBlock: undefined };
}

export const TryExcept: MessageFns<TryExcept> = {
  encode(message: TryExcept, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.handlers) {
      ExceptHandler.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.tryBlock !== undefined) {
      Block.encode(message.tryBlock, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TryExcept {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTryExcept();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.handlers.push(ExceptHandler.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tryBlock = Block.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TryExcept {
    return {
      handlers: globalThis.Array.isArray(object?.handlers)
        ? object.handlers.map((e: any) => ExceptHandler.fromJSON(e))
        : [],
      tryBlock: isSet(object.tryBlock)
        ? Block.fromJSON(object.tryBlock)
        : isSet(object.try_block)
        ? Block.fromJSON(object.try_block)
        : undefined,
    };
  },

  toJSON(message: TryExcept): unknown {
    const obj: any = {};
    if (message.handlers?.length) {
      obj.handlers = message.handlers.map((e) => ExceptHandler.toJSON(e));
    }
    if (message.tryBlock !== undefined) {
      obj.tryBlock = Block.toJSON(message.tryBlock);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TryExcept>, I>>(base?: I): TryExcept {
    return TryExcept.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TryExcept>, I>>(object: I): TryExcept {
    const message = createBaseTryExcept();
    message.handlers = object.handlers?.map((e) => ExceptHandler.fromPartial(e)) || [];
    message.tryBlock = (object.tryBlock !== undefined && object.tryBlock !== null)
      ? Block.fromPartial(object.tryBlock)
      : undefined;
    return message;
  },
};

function createBaseExceptHandler(): ExceptHandler {
  return { exceptionTypes: [], span: undefined, blockBody: undefined, exceptionVar: undefined };
}

export const ExceptHandler: MessageFns<ExceptHandler> = {
  encode(message: ExceptHandler, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.exceptionTypes) {
      writer.uint32(10).string(v!);
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(18).fork()).join();
    }
    if (message.blockBody !== undefined) {
      Block.encode(message.blockBody, writer.uint32(26).fork()).join();
    }
    if (message.exceptionVar !== undefined) {
      writer.uint32(34).string(message.exceptionVar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExceptHandler {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExceptHandler();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exceptionTypes.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.blockBody = Block.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.exceptionVar = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExceptHandler {
    return {
      exceptionTypes: globalThis.Array.isArray(object?.exceptionTypes)
        ? object.exceptionTypes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.exception_types)
        ? object.exception_types.map((e: any) => globalThis.String(e))
        : [],
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
      blockBody: isSet(object.blockBody)
        ? Block.fromJSON(object.blockBody)
        : isSet(object.block_body)
        ? Block.fromJSON(object.block_body)
        : undefined,
      exceptionVar: isSet(object.exceptionVar)
        ? globalThis.String(object.exceptionVar)
        : isSet(object.exception_var)
        ? globalThis.String(object.exception_var)
        : undefined,
    };
  },

  toJSON(message: ExceptHandler): unknown {
    const obj: any = {};
    if (message.exceptionTypes?.length) {
      obj.exceptionTypes = message.exceptionTypes;
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    if (message.blockBody !== undefined) {
      obj.blockBody = Block.toJSON(message.blockBody);
    }
    if (message.exceptionVar !== undefined) {
      obj.exceptionVar = message.exceptionVar;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExceptHandler>, I>>(base?: I): ExceptHandler {
    return ExceptHandler.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExceptHandler>, I>>(object: I): ExceptHandler {
    const message = createBaseExceptHandler();
    message.exceptionTypes = object.exceptionTypes?.map((e) => e) || [];
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    message.blockBody = (object.blockBody !== undefined && object.blockBody !== null)
      ? Block.fromPartial(object.blockBody)
      : undefined;
    message.exceptionVar = object.exceptionVar ?? undefined;
    return message;
  },
};

function createBaseReturnStmt(): ReturnStmt {
  return { value: undefined };
}

export const ReturnStmt: MessageFns<ReturnStmt> = {
  encode(message: ReturnStmt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value !== undefined) {
      Expr.encode(message.value, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReturnStmt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReturnStmt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReturnStmt {
    return { value: isSet(object.value) ? Expr.fromJSON(object.value) : undefined };
  },

  toJSON(message: ReturnStmt): unknown {
    const obj: any = {};
    if (message.value !== undefined) {
      obj.value = Expr.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReturnStmt>, I>>(base?: I): ReturnStmt {
    return ReturnStmt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReturnStmt>, I>>(object: I): ReturnStmt {
    const message = createBaseReturnStmt();
    message.value = (object.value !== undefined && object.value !== null) ? Expr.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseExprStmt(): ExprStmt {
  return { expr: undefined };
}

export const ExprStmt: MessageFns<ExprStmt> = {
  encode(message: ExprStmt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.expr !== undefined) {
      Expr.encode(message.expr, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExprStmt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExprStmt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.expr = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExprStmt {
    return { expr: isSet(object.expr) ? Expr.fromJSON(object.expr) : undefined };
  },

  toJSON(message: ExprStmt): unknown {
    const obj: any = {};
    if (message.expr !== undefined) {
      obj.expr = Expr.toJSON(message.expr);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExprStmt>, I>>(base?: I): ExprStmt {
    return ExprStmt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExprStmt>, I>>(object: I): ExprStmt {
    const message = createBaseExprStmt();
    message.expr = (object.expr !== undefined && object.expr !== null) ? Expr.fromPartial(object.expr) : undefined;
    return message;
  },
};

function createBaseSleepStmt(): SleepStmt {
  return { duration: undefined };
}

export const SleepStmt: MessageFns<SleepStmt> = {
  encode(message: SleepStmt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.duration !== undefined) {
      Expr.encode(message.duration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SleepStmt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSleepStmt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.duration = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SleepStmt {
    return { duration: isSet(object.duration) ? Expr.fromJSON(object.duration) : undefined };
  },

  toJSON(message: SleepStmt): unknown {
    const obj: any = {};
    if (message.duration !== undefined) {
      obj.duration = Expr.toJSON(message.duration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SleepStmt>, I>>(base?: I): SleepStmt {
    return SleepStmt.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SleepStmt>, I>>(object: I): SleepStmt {
    const message = createBaseSleepStmt();
    message.duration = (object.duration !== undefined && object.duration !== null)
      ? Expr.fromPartial(object.duration)
      : undefined;
    return message;
  },
};

function createBaseExpr(): Expr {
  return { kind: undefined, span: undefined };
}

export const Expr: MessageFns<Expr> = {
  encode(message: Expr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "literal":
        Literal.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "variable":
        Variable.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
      case "binaryOp":
        BinaryOp.encode(message.kind.value, writer.uint32(26).fork()).join();
        break;
      case "unaryOp":
        UnaryOp.encode(message.kind.value, writer.uint32(34).fork()).join();
        break;
      case "list":
        ListExpr.encode(message.kind.value, writer.uint32(42).fork()).join();
        break;
      case "dict":
        DictExpr.encode(message.kind.value, writer.uint32(50).fork()).join();
        break;
      case "index":
        IndexAccess.encode(message.kind.value, writer.uint32(58).fork()).join();
        break;
      case "dot":
        DotAccess.encode(message.kind.value, writer.uint32(66).fork()).join();
        break;
      case "functionCall":
        FunctionCall.encode(message.kind.value, writer.uint32(74).fork()).join();
        break;
      case "actionCall":
        ActionCall.encode(message.kind.value, writer.uint32(82).fork()).join();
        break;
      case "parallelExpr":
        ParallelExpr.encode(message.kind.value, writer.uint32(98).fork()).join();
        break;
      case "spreadExpr":
        SpreadExpr.encode(message.kind.value, writer.uint32(106).fork()).join();
        break;
    }
    if (message.span !== undefined) {
      Span.encode(message.span, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Expr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "literal", value: Literal.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "variable", value: Variable.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = { $case: "binaryOp", value: BinaryOp.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.kind = { $case: "unaryOp", value: UnaryOp.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.kind = { $case: "list", value: ListExpr.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.kind = { $case: "dict", value: DictExpr.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.kind = { $case: "index", value: IndexAccess.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.kind = { $case: "dot", value: DotAccess.decode(reader, reader.uint32()) };
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.kind = { $case: "functionCall", value: FunctionCall.decode(reader, reader.uint32()) };
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.kind = { $case: "actionCall", value: ActionCall.decode(reader, reader.uint32()) };
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.kind = { $case: "parallelExpr", value: ParallelExpr.decode(reader, reader.uint32()) };
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.kind = { $case: "spreadExpr", value: SpreadExpr.decode(reader, reader.uint32()) };
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.span = Span.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Expr {
    return {
      kind: isSet(object.literal)
        ? { $case: "literal", value: Literal.fromJSON(object.literal) }
        : isSet(object.variable)
        ? { $case: "variable", value: Variable.fromJSON(object.variable) }
        : isSet(object.binaryOp)
        ? { $case: "binaryOp", value: BinaryOp.fromJSON(object.binaryOp) }
        : isSet(object.binary_op)
        ? { $case: "binaryOp", value: BinaryOp.fromJSON(object.binary_op) }
        : isSet(object.unaryOp)
        ? { $case: "unaryOp", value: UnaryOp.fromJSON(object.unaryOp) }
        : isSet(object.unary_op)
        ? { $case: "unaryOp", value: UnaryOp.fromJSON(object.unary_op) }
        : isSet(object.list)
        ? { $case: "list", value: ListExpr.fromJSON(object.list) }
        : isSet(object.dict)
        ? { $case: "dict", value: DictExpr.fromJSON(object.dict) }
        : isSet(object.index)
        ? { $case: "index", value: IndexAccess.fromJSON(object.index) }
        : isSet(object.dot)
        ? { $case: "dot", value: DotAccess.fromJSON(object.dot) }
        : isSet(object.functionCall)
        ? { $case: "functionCall", value: FunctionCall.fromJSON(object.functionCall) }
        : isSet(object.function_call)
        ? { $case: "functionCall", value: FunctionCall.fromJSON(object.function_call) }
        : isSet(object.actionCall)
        ? { $case: "actionCall", value: ActionCall.fromJSON(object.actionCall) }
        : isSet(object.action_call)
        ? { $case: "actionCall", value: ActionCall.fromJSON(object.action_call) }
        : isSet(object.parallelExpr)
        ? { $case: "parallelExpr", value: ParallelExpr.fromJSON(object.parallelExpr) }
        : isSet(object.parallel_expr)
        ? { $case: "parallelExpr", value: ParallelExpr.fromJSON(object.parallel_expr) }
        : isSet(object.spreadExpr)
        ? { $case: "spreadExpr", value: SpreadExpr.fromJSON(object.spreadExpr) }
        : isSet(object.spread_expr)
        ? { $case: "spreadExpr", value: SpreadExpr.fromJSON(object.spread_expr) }
        : undefined,
      span: isSet(object.span) ? Span.fromJSON(object.span) : undefined,
    };
  },

  toJSON(message: Expr): unknown {
    const obj: any = {};
    if (message.kind?.$case === "literal") {
      obj.literal = Literal.toJSON(message.kind.value);
    } else if (message.kind?.$case === "variable") {
      obj.variable = Variable.toJSON(message.kind.value);
    } else if (message.kind?.$case === "binaryOp") {
      obj.binaryOp = BinaryOp.toJSON(message.kind.value);
    } else if (message.kind?.$case === "unaryOp") {
      obj.unaryOp = UnaryOp.toJSON(message.kind.value);
    } else if (message.kind?.$case === "list") {
      obj.list = ListExpr.toJSON(message.kind.value);
    } else if (message.kind?.$case === "dict") {
      obj.dict = DictExpr.toJSON(message.kind.value);
    } else if (message.kind?.$case === "index") {
      obj.index = IndexAccess.toJSON(message.kind.value);
    } else if (message.kind?.$case === "dot") {
      obj.dot = DotAccess.toJSON(message.kind.value);
    } else if (message.kind?.$case === "functionCall") {
      obj.functionCall = FunctionCall.toJSON(message.kind.value);
    } else if (message.kind?.$case === "actionCall") {
      obj.actionCall = ActionCall.toJSON(message.kind.value);
    } else if (message.kind?.$case === "parallelExpr") {
      obj.parallelExpr = ParallelExpr.toJSON(message.kind.value);
    } else if (message.kind?.$case === "spreadExpr") {
      obj.spreadExpr = SpreadExpr.toJSON(message.kind.value);
    }
    if (message.span !== undefined) {
      obj.span = Span.toJSON(message.span);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Expr>, I>>(base?: I): Expr {
    return Expr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Expr>, I>>(object: I): Expr {
    const message = createBaseExpr();
    switch (object.kind?.$case) {
      case "literal": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "literal", value: Literal.fromPartial(object.kind.value) };
        }
        break;
      }
      case "variable": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "variable", value: Variable.fromPartial(object.kind.value) };
        }
        break;
      }
      case "binaryOp": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "binaryOp", value: BinaryOp.fromPartial(object.kind.value) };
        }
        break;
      }
      case "unaryOp": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "unaryOp", value: UnaryOp.fromPartial(object.kind.value) };
        }
        break;
      }
      case "list": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "list", value: ListExpr.fromPartial(object.kind.value) };
        }
        break;
      }
      case "dict": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "dict", value: DictExpr.fromPartial(object.kind.value) };
        }
        break;
      }
      case "index": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "index", value: IndexAccess.fromPartial(object.kind.value) };
        }
        break;
      }
      case "dot": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "dot", value: DotAccess.fromPartial(object.kind.value) };
        }
        break;
      }
      case "functionCall": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "functionCall", value: FunctionCall.fromPartial(object.kind.value) };
        }
        break;
      }
      case "actionCall": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "actionCall", value: ActionCall.fromPartial(object.kind.value) };
        }
        break;
      }
      case "parallelExpr": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "parallelExpr", value: ParallelExpr.fromPartial(object.kind.value) };
        }
        break;
      }
      case "spreadExpr": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "spreadExpr", value: SpreadExpr.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    message.span = (object.span !== undefined && object.span !== null) ? Span.fromPartial(object.span) : undefined;
    return message;
  },
};

function createBaseLiteral(): Literal {
  return { value: undefined };
}

export const Literal: MessageFns<Literal> = {
  encode(message: Literal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.value?.$case) {
      case "intValue":
        if (BigInt.asIntN(64, message.value.value) !== message.value.value) {
          throw new globalThis.Error("value provided for field message.value.value of type int64 too large");
        }
        writer.uint32(8).int64(message.value.value);
        break;
      case "floatValue":
        writer.uint32(17).double(message.value.value);
        break;
      case "stringValue":
        writer.uint32(26).string(message.value.value);
        break;
      case "boolValue":
        writer.uint32(32).bool(message.value.value);
        break;
      case "isNone":
        writer.uint32(40).bool(message.value.value);
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Literal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLiteral();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.value = { $case: "intValue", value: reader.int64() as bigint };
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.value = { $case: "floatValue", value: reader.double() };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.value = { $case: "stringValue", value: reader.string() };
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.value = { $case: "boolValue", value: reader.bool() };
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.value = { $case: "isNone", value: reader.bool() };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Literal {
    return {
      value: isSet(object.intValue)
        ? { $case: "intValue", value: BigInt(object.intValue) }
        : isSet(object.int_value)
        ? { $case: "intValue", value: BigInt(object.int_value) }
        : isSet(object.floatValue)
        ? { $case: "floatValue", value: globalThis.Number(object.floatValue) }
        : isSet(object.float_value)
        ? { $case: "floatValue", value: globalThis.Number(object.float_value) }
        : isSet(object.stringValue)
        ? { $case: "stringValue", value: globalThis.String(object.stringValue) }
        : isSet(object.string_value)
        ? { $case: "stringValue", value: globalThis.String(object.string_value) }
        : isSet(object.boolValue)
        ? { $case: "boolValue", value: globalThis.Boolean(object.boolValue) }
        : isSet(object.bool_value)
        ? { $case: "boolValue", value: globalThis.Boolean(object.bool_value) }
        : isSet(object.isNone)
        ? { $case: "isNone", value: globalThis.Boolean(object.isNone) }
        : isSet(object.is_none)
        ? { $case: "isNone", value: globalThis.Boolean(object.is_none) }
        : undefined,
    };
  },

  toJSON(message: Literal): unknown {
    const obj: any = {};
    if (message.value?.$case === "intValue") {
      obj.intValue = message.value.value.toString();
    } else if (message.value?.$case === "floatValue") {
      obj.floatValue = message.value.value;
    } else if (message.value?.$case === "stringValue") {
      obj.stringValue = message.value.value;
    } else if (message.value?.$case === "boolValue") {
      obj.boolValue = message.value.value;
    } else if (message.value?.$case === "isNone") {
      obj.isNone = message.value.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Literal>, I>>(base?: I): Literal {
    return Literal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Literal>, I>>(object: I): Literal {
    const message = createBaseLiteral();
    switch (object.value?.$case) {
      case "intValue": {
        if (object.value?.value !== undefined && object.value?.value !== null) {
          message.value = { $case: "intValue", value: BigInt(object.value.value) };
        }
        break;
      }
      case "floatValue": {
        if (object.value?.value !== undefined && object.value?.value !== null) {
          message.value = { $case: "floatValue", value: object.value.value };
        }
        break;
      }
      case "stringValue": {
        if (object.value?.value !== undefined && object.value?.value !== null) {
          message.value = { $case: "stringValue", value: object.value.value };
        }
        break;
      }
      case "boolValue": {
        if (object.value?.value !== undefined && object.value?.value !== null) {
          message.value = { $case: "boolValue", value: object.value.value };
        }
        break;
      }
      case "isNone": {
        if (object.value?.value !== undefined && object.value?.value !== null) {
          message.value = { $case: "isNone", value: object.value.value };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseVariable(): Variable {
  return { name: "" };
}

export const Variable: MessageFns<Variable> = {
  encode(message: Variable, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Variable {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVariable();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Variable {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: Variable): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Variable>, I>>(base?: I): Variable {
    return Variable.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Variable>, I>>(object: I): Variable {
    const message = createBaseVariable();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseBinaryOp(): BinaryOp {
  return { left: undefined, op: 0, right: undefined };
}

export const BinaryOp: MessageFns<BinaryOp> = {
  encode(message: BinaryOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.left !== undefined) {
      Expr.encode(message.left, writer.uint32(10).fork()).join();
    }
    if (message.op !== 0) {
      writer.uint32(16).int32(message.op);
    }
    if (message.right !== undefined) {
      Expr.encode(message.right, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BinaryOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBinaryOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.left = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.right = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BinaryOp {
    return {
      left: isSet(object.left) ? Expr.fromJSON(object.left) : undefined,
      op: isSet(object.op) ? binaryOperatorFromJSON(object.op) : 0,
      right: isSet(object.right) ? Expr.fromJSON(object.right) : undefined,
    };
  },

  toJSON(message: BinaryOp): unknown {
    const obj: any = {};
    if (message.left !== undefined) {
      obj.left = Expr.toJSON(message.left);
    }
    if (message.op !== 0) {
      obj.op = binaryOperatorToJSON(message.op);
    }
    if (message.right !== undefined) {
      obj.right = Expr.toJSON(message.right);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BinaryOp>, I>>(base?: I): BinaryOp {
    return BinaryOp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BinaryOp>, I>>(object: I): BinaryOp {
    const message = createBaseBinaryOp();
    message.left = (object.left !== undefined && object.left !== null) ? Expr.fromPartial(object.left) : undefined;
    message.op = object.op ?? 0;
    message.right = (object.right !== undefined && object.right !== null) ? Expr.fromPartial(object.right) : undefined;
    return message;
  },
};

function createBaseUnaryOp(): UnaryOp {
  return { op: 0, operand: undefined };
}

export const UnaryOp: MessageFns<UnaryOp> = {
  encode(message: UnaryOp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.op !== 0) {
      writer.uint32(8).int32(message.op);
    }
    if (message.operand !== undefined) {
      Expr.encode(message.operand, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnaryOp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnaryOp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.operand = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnaryOp {
    return {
      op: isSet(object.op) ? unaryOperatorFromJSON(object.op) : 0,
      operand: isSet(object.operand) ? Expr.fromJSON(object.operand) : undefined,
    };
  },

  toJSON(message: UnaryOp): unknown {
    const obj: any = {};
    if (message.op !== 0) {
      obj.op = unaryOperatorToJSON(message.op);
    }
    if (message.operand !== undefined) {
      obj.operand = Expr.toJSON(message.operand);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnaryOp>, I>>(base?: I): UnaryOp {
    return UnaryOp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnaryOp>, I>>(object: I): UnaryOp {
    const message = createBaseUnaryOp();
    message.op = object.op ?? 0;
    message.operand = (object.operand !== undefined && object.operand !== null)
      ? Expr.fromPartial(object.operand)
      : undefined;
    return message;
  },
};

function createBaseListExpr(): ListExpr {
  return { elements: [] };
}

export const ListExpr: MessageFns<ListExpr> = {
  encode(message: ListExpr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.elements) {
      Expr.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListExpr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListExpr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.elements.push(Expr.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListExpr {
    return {
      elements: globalThis.Array.isArray(object?.elements) ? object.elements.map((e: any) => Expr.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListExpr): unknown {
    const obj: any = {};
    if (message.elements?.length) {
      obj.elements = message.elements.map((e) => Expr.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListExpr>, I>>(base?: I): ListExpr {
    return ListExpr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListExpr>, I>>(object: I): ListExpr {
    const message = createBaseListExpr();
    message.elements = object.elements?.map((e) => Expr.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDictExpr(): DictExpr {
  return { entries: [] };
}

export const DictExpr: MessageFns<DictExpr> = {
  encode(message: DictExpr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      DictEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DictExpr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDictExpr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(DictEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DictExpr {
    return {
      entries: globalThis.Array.isArray(object?.entries) ? object.entries.map((e: any) => DictEntry.fromJSON(e)) : [],
    };
  },

  toJSON(message: DictExpr): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => DictEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DictExpr>, I>>(base?: I): DictExpr {
    return DictExpr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DictExpr>, I>>(object: I): DictExpr {
    const message = createBaseDictExpr();
    message.entries = object.entries?.map((e) => DictEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDictEntry(): DictEntry {
  return { key: undefined, value: undefined };
}

export const DictEntry: MessageFns<DictEntry> = {
  encode(message: DictEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== undefined) {
      Expr.encode(message.key, writer.uint32(10).fork()).join();
    }
    if (message.value !== undefined) {
      Expr.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DictEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDictEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DictEntry {
    return {
      key: isSet(object.key) ? Expr.fromJSON(object.key) : undefined,
      value: isSet(object.value) ? Expr.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: DictEntry): unknown {
    const obj: any = {};
    if (message.key !== undefined) {
      obj.key = Expr.toJSON(message.key);
    }
    if (message.value !== undefined) {
      obj.value = Expr.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DictEntry>, I>>(base?: I): DictEntry {
    return DictEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DictEntry>, I>>(object: I): DictEntry {
    const message = createBaseDictEntry();
    message.key = (object.key !== undefined && object.key !== null) ? Expr.fromPartial(object.key) : undefined;
    message.value = (object.value !== undefined && object.value !== null) ? Expr.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseIndexAccess(): IndexAccess {
  return { object: undefined, index: undefined };
}

export const IndexAccess: MessageFns<IndexAccess> = {
  encode(message: IndexAccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.object !== undefined) {
      Expr.encode(message.object, writer.uint32(10).fork()).join();
    }
    if (message.index !== undefined) {
      Expr.encode(message.index, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexAccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexAccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.object = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.index = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexAccess {
    return {
      object: isSet(object.object) ? Expr.fromJSON(object.object) : undefined,
      index: isSet(object.index) ? Expr.fromJSON(object.index) : undefined,
    };
  },

  toJSON(message: IndexAccess): unknown {
    const obj: any = {};
    if (message.object !== undefined) {
      obj.object = Expr.toJSON(message.object);
    }
    if (message.index !== undefined) {
      obj.index = Expr.toJSON(message.index);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexAccess>, I>>(base?: I): IndexAccess {
    return IndexAccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexAccess>, I>>(object: I): IndexAccess {
    const message = createBaseIndexAccess();
    message.object = (object.object !== undefined && object.object !== null)
      ? Expr.fromPartial(object.object)
      : undefined;
    message.index = (object.index !== undefined && object.index !== null) ? Expr.fromPartial(object.index) : undefined;
    return message;
  },
};

function createBaseDotAccess(): DotAccess {
  return { object: undefined, attribute: "" };
}

export const DotAccess: MessageFns<DotAccess> = {
  encode(message: DotAccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.object !== undefined) {
      Expr.encode(message.object, writer.uint32(10).fork()).join();
    }
    if (message.attribute !== "") {
      writer.uint32(18).string(message.attribute);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DotAccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDotAccess();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.object = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.attribute = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DotAccess {
    return {
      object: isSet(object.object) ? Expr.fromJSON(object.object) : undefined,
      attribute: isSet(object.attribute) ? globalThis.String(object.attribute) : "",
    };
  },

  toJSON(message: DotAccess): unknown {
    const obj: any = {};
    if (message.object !== undefined) {
      obj.object = Expr.toJSON(message.object);
    }
    if (message.attribute !== "") {
      obj.attribute = message.attribute;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DotAccess>, I>>(base?: I): DotAccess {
    return DotAccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DotAccess>, I>>(object: I): DotAccess {
    const message = createBaseDotAccess();
    message.object = (object.object !== undefined && object.object !== null)
      ? Expr.fromPartial(object.object)
      : undefined;
    message.attribute = object.attribute ?? "";
    return message;
  },
};

function createBaseFunctionCall(): FunctionCall {
  return { name: "", args: [], kwargs: [], globalFunction: 0 };
}

export const FunctionCall: MessageFns<FunctionCall> = {
  encode(message: FunctionCall, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.args) {
      Expr.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.kwargs) {
      Kwarg.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.globalFunction !== 0) {
      writer.uint32(32).int32(message.globalFunction);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FunctionCall {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFunctionCall();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.args.push(Expr.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kwargs.push(Kwarg.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.globalFunction = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FunctionCall {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => Expr.fromJSON(e)) : [],
      kwargs: globalThis.Array.isArray(object?.kwargs) ? object.kwargs.map((e: any) => Kwarg.fromJSON(e)) : [],
      globalFunction: isSet(object.globalFunction)
        ? globalFunctionFromJSON(object.globalFunction)
        : isSet(object.global_function)
        ? globalFunctionFromJSON(object.global_function)
        : 0,
    };
  },

  toJSON(message: FunctionCall): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.args?.length) {
      obj.args = message.args.map((e) => Expr.toJSON(e));
    }
    if (message.kwargs?.length) {
      obj.kwargs = message.kwargs.map((e) => Kwarg.toJSON(e));
    }
    if (message.globalFunction !== 0) {
      obj.globalFunction = globalFunctionToJSON(message.globalFunction);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FunctionCall>, I>>(base?: I): FunctionCall {
    return FunctionCall.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FunctionCall>, I>>(object: I): FunctionCall {
    const message = createBaseFunctionCall();
    message.name = object.name ?? "";
    message.args = object.args?.map((e) => Expr.fromPartial(e)) || [];
    message.kwargs = object.kwargs?.map((e) => Kwarg.fromPartial(e)) || [];
    message.globalFunction = object.globalFunction ?? 0;
    return message;
  },
};

function createBaseKwarg(): Kwarg {
  return { name: "", value: undefined };
}

export const Kwarg: MessageFns<Kwarg> = {
  encode(message: Kwarg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.value !== undefined) {
      Expr.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Kwarg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKwarg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Expr.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Kwarg {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? Expr.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: Kwarg): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== undefined) {
      obj.value = Expr.toJSON(message.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Kwarg>, I>>(base?: I): Kwarg {
    return Kwarg.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Kwarg>, I>>(object: I): Kwarg {
    const message = createBaseKwarg();
    message.name = object.name ?? "";
    message.value = (object.value !== undefined && object.value !== null) ? Expr.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseParallelExpr(): ParallelExpr {
  return { calls: [] };
}

export const ParallelExpr: MessageFns<ParallelExpr> = {
  encode(message: ParallelExpr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.calls) {
      Call.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParallelExpr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParallelExpr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.calls.push(Call.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParallelExpr {
    return { calls: globalThis.Array.isArray(object?.calls) ? object.calls.map((e: any) => Call.fromJSON(e)) : [] };
  },

  toJSON(message: ParallelExpr): unknown {
    const obj: any = {};
    if (message.calls?.length) {
      obj.calls = message.calls.map((e) => Call.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParallelExpr>, I>>(base?: I): ParallelExpr {
    return ParallelExpr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParallelExpr>, I>>(object: I): ParallelExpr {
    const message = createBaseParallelExpr();
    message.calls = object.calls?.map((e) => Call.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSpreadExpr(): SpreadExpr {
  return { collection: undefined, loopVar: "", action: undefined };
}

export const SpreadExpr: MessageFns<SpreadExpr> = {
  encode(message: SpreadExpr, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collection !== undefined) {
      Expr.encode(message.collection, writer.uint32(10).fork()).join();
    }
    if (message.loopVar !== "") {
      writer.uint32(18).string(message.loopVar);
    }
    if (message.action !== undefined) {
      ActionCall.encode(message.action, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpreadExpr {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpreadExpr();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.collection = Expr.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.loopVar = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.action = ActionCall.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpreadExpr {
    return {
      collection: isSet(object.collection) ? Expr.fromJSON(object.collection) : undefined,
      loopVar: isSet(object.loopVar)
        ? globalThis.String(object.loopVar)
        : isSet(object.loop_var)
        ? globalThis.String(object.loop_var)
        : "",
      action: isSet(object.action) ? ActionCall.fromJSON(object.action) : undefined,
    };
  },

  toJSON(message: SpreadExpr): unknown {
    const obj: any = {};
    if (message.collection !== undefined) {
      obj.collection = Expr.toJSON(message.collection);
    }
    if (message.loopVar !== "") {
      obj.loopVar = message.loopVar;
    }
    if (message.action !== undefined) {
      obj.action = ActionCall.toJSON(message.action);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpreadExpr>, I>>(base?: I): SpreadExpr {
    return SpreadExpr.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpreadExpr>, I>>(object: I): SpreadExpr {
    const message = createBaseSpreadExpr();
    message.collection = (object.collection !== undefined && object.collection !== null)
      ? Expr.fromPartial(object.collection)
      : undefined;
    message.loopVar = object.loopVar ?? "";
    message.action = (object.action !== undefined && object.action !== null)
      ? ActionCall.fromPartial(object.action)
      : undefined;
    return message;
  },
};

function createBasePolicyBracket(): PolicyBracket {
  return { kind: undefined };
}

export const PolicyBracket: MessageFns<PolicyBracket> = {
  encode(message: PolicyBracket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "retry":
        RetryPolicy.encode(message.kind.value, writer.uint32(10).fork()).join();
        break;
      case "timeout":
        TimeoutPolicy.encode(message.kind.value, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PolicyBracket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePolicyBracket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "retry", value: RetryPolicy.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "timeout", value: TimeoutPolicy.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PolicyBracket {
    return {
      kind: isSet(object.retry)
        ? { $case: "retry", value: RetryPolicy.fromJSON(object.retry) }
        : isSet(object.timeout)
        ? { $case: "timeout", value: TimeoutPolicy.fromJSON(object.timeout) }
        : undefined,
    };
  },

  toJSON(message: PolicyBracket): unknown {
    const obj: any = {};
    if (message.kind?.$case === "retry") {
      obj.retry = RetryPolicy.toJSON(message.kind.value);
    } else if (message.kind?.$case === "timeout") {
      obj.timeout = TimeoutPolicy.toJSON(message.kind.value);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PolicyBracket>, I>>(base?: I): PolicyBracket {
    return PolicyBracket.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PolicyBracket>, I>>(object: I): PolicyBracket {
    const message = createBasePolicyBracket();
    switch (object.kind?.$case) {
      case "retry": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "retry", value: RetryPolicy.fromPartial(object.kind.value) };
        }
        break;
      }
      case "timeout": {
        if (object.kind?.value !== undefined && object.kind?.value !== null) {
          message.kind = { $case: "timeout", value: TimeoutPolicy.fromPartial(object.kind.value) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseRetryPolicy(): RetryPolicy {
  return { exceptionTypes: [], maxRetries: 0, backoff: undefined };
}

export const RetryPolicy: MessageFns<RetryPolicy> = {
  encode(message: RetryPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.exceptionTypes) {
      writer.uint32(10).string(v!);
    }
    if (message.maxRetries !== 0) {
      writer.uint32(16).uint32(message.maxRetries);
    }
    if (message.backoff !== undefined) {
      Duration.encode(message.backoff, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RetryPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRetryPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.exceptionTypes.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxRetries = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.backoff = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RetryPolicy {
    return {
      exceptionTypes: globalThis.Array.isArray(object?.exceptionTypes)
        ? object.exceptionTypes.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.exception_types)
        ? object.exception_types.map((e: any) => globalThis.String(e))
        : [],
      maxRetries: isSet(object.maxRetries)
        ? globalThis.Number(object.maxRetries)
        : isSet(object.max_retries)
        ? globalThis.Number(object.max_retries)
        : 0,
      backoff: isSet(object.backoff) ? Duration.fromJSON(object.backoff) : undefined,
    };
  },

  toJSON(message: RetryPolicy): unknown {
    const obj: any = {};
    if (message.exceptionTypes?.length) {
      obj.exceptionTypes = message.exceptionTypes;
    }
    if (message.maxRetries !== 0) {
      obj.maxRetries = Math.round(message.maxRetries);
    }
    if (message.backoff !== undefined) {
      obj.backoff = Duration.toJSON(message.backoff);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RetryPolicy>, I>>(base?: I): RetryPolicy {
    return RetryPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RetryPolicy>, I>>(object: I): RetryPolicy {
    const message = createBaseRetryPolicy();
    message.exceptionTypes = object.exceptionTypes?.map((e) => e) || [];
    message.maxRetries = object.maxRetries ?? 0;
    message.backoff = (object.backoff !== undefined && object.backoff !== null)
      ? Duration.fromPartial(object.backoff)
      : undefined;
    return message;
  },
};

function createBaseTimeoutPolicy(): TimeoutPolicy {
  return { timeout: undefined };
}

export const TimeoutPolicy: MessageFns<TimeoutPolicy> = {
  encode(message: TimeoutPolicy, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timeout !== undefined) {
      Duration.encode(message.timeout, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeoutPolicy {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeoutPolicy();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timeout = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeoutPolicy {
    return { timeout: isSet(object.timeout) ? Duration.fromJSON(object.timeout) : undefined };
  },

  toJSON(message: TimeoutPolicy): unknown {
    const obj: any = {};
    if (message.timeout !== undefined) {
      obj.timeout = Duration.toJSON(message.timeout);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeoutPolicy>, I>>(base?: I): TimeoutPolicy {
    return TimeoutPolicy.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeoutPolicy>, I>>(object: I): TimeoutPolicy {
    const message = createBaseTimeoutPolicy();
    message.timeout = (object.timeout !== undefined && object.timeout !== null)
      ? Duration.fromPartial(object.timeout)
      : undefined;
    return message;
  },
};

function createBaseDuration(): Duration {
  return { seconds: 0n };
}

export const Duration: MessageFns<Duration> = {
  encode(message: Duration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.seconds !== 0n) {
      if (BigInt.asUintN(64, message.seconds) !== message.seconds) {
        throw new globalThis.Error("value provided for field message.seconds of type uint64 too large");
      }
      writer.uint32(8).uint64(message.seconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Duration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDuration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.seconds = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Duration {
    return { seconds: isSet(object.seconds) ? BigInt(object.seconds) : 0n };
  },

  toJSON(message: Duration): unknown {
    const obj: any = {};
    if (message.seconds !== 0n) {
      obj.seconds = message.seconds.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Duration>, I>>(base?: I): Duration {
    return Duration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Duration>, I>>(object: I): Duration {
    const message = createBaseDuration();
    message.seconds = (object.seconds !== undefined && object.seconds !== null) ? BigInt(object.seconds) : 0n;
    return message;
  },
};

function createBaseSpan(): Span {
  return { startLine: 0, startCol: 0, endLine: 0, endCol: 0 };
}

export const Span: MessageFns<Span> = {
  encode(message: Span, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startLine !== 0) {
      writer.uint32(8).uint32(message.startLine);
    }
    if (message.startCol !== 0) {
      writer.uint32(16).uint32(message.startCol);
    }
    if (message.endLine !== 0) {
      writer.uint32(24).uint32(message.endLine);
    }
    if (message.endCol !== 0) {
      writer.uint32(32).uint32(message.endCol);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Span {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.startLine = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.startCol = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.endLine = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.endCol = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Span {
    return {
      startLine: isSet(object.startLine)
        ? globalThis.Number(object.startLine)
        : isSet(object.start_line)
        ? globalThis.Number(object.start_line)
        : 0,
      startCol: isSet(object.startCol)
        ? globalThis.Number(object.startCol)
        : isSet(object.start_col)
        ? globalThis.Number(object.start_col)
        : 0,
      endLine: isSet(object.endLine)
        ? globalThis.Number(object.endLine)
        : isSet(object.end_line)
        ? globalThis.Number(object.end_line)
        : 0,
      endCol: isSet(object.endCol)
        ? globalThis.Number(object.endCol)
        : isSet(object.end_col)
        ? globalThis.Number(object.end_col)
        : 0,
    };
  },

  toJSON(message: Span): unknown {
    const obj: any = {};
    if (message.startLine !== 0) {
      obj.startLine = Math.round(message.startLine);
    }
    if (message.startCol !== 0) {
      obj.startCol = Math.round(message.startCol);
    }
    if (message.endLine !== 0) {
      obj.endLine = Math.round(message.endLine);
    }
    if (message.endCol !== 0) {
      obj.endCol = Math.round(message.endCol);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Span>, I>>(base?: I): Span {
    return Span.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Span>, I>>(object: I): Span {
    const message = createBaseSpan();
    message.startLine = object.startLine ?? 0;
    message.startCol = object.startCol ?? 0;
    message.endLine = object.endLine ?? 0;
    message.endCol = object.endCol ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string; value: unknown } ? { $case: T["$case"]; value?: DeepPartial<T["value"]> }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
