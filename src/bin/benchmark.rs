//! Benchmark CLI - stubbed out pending migration to new store/scheduler model.

use anyhow::Result;

fn main() -> Result<()> {
    eprintln!("Benchmark CLI is temporarily disabled pending migration to new store/scheduler model.");
    eprintln!("Please use the integration tests for now.");
    std::process::exit(1);
}
